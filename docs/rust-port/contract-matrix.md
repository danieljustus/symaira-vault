# Rust port contract matrix

> **Read this matrix together with
> [`port-coverage-20260916.md`](port-coverage-20260916.md).** Counting `PASS`
> rows overstates how close a cutover is: the rows are concentrated in the
> library layer, while `internal/mcp` (the largest subsystem in the repository),
> the HTTP server, and many CLI command paths remain unimplemented in Rust.
> The coverage counts in the linked audit are a dated snapshot, not the current
> branch. No row's status is affected -- every `PASS` is backed by the
> evidence it claims -- but a `TODO` row waiting on a *port* is not comparable
> work to one waiting on a *fixture*, and the matrix alone does not distinguish
> them.

`TODO` means the contract is identified but does not yet have a language-neutral
fixture and Rust parity test. `PASS` requires an executable test in CI; prose or
compilation is not evidence. Platform-scoped `PASS` rows also require native
runtime evidence for each claimed platform; a host-only run is not platform
coverage. The Go oracle is commit `caadd5e` / release
`v0.22.1` until deliberately advanced before the first implementation PR. The
`AUDIT-001`/`AUDIT-002` vectors deliberately advance only their production-Go
oracle to commit `a57f565a`; `APPROVAL-001` deliberately advances only its
production-Go oracle to commit `a518124f` (merged PR #1048), because that
seam did not exist at the frozen baseline; `STORE-002` deliberately advances
only its production-Go oracle to commit `fe098b91` (release "unreleased"),
pre-existing but previously undocumented here; `POLICY-001` deliberately
advances only its production-Go oracle to commit `f195aab` (release
"unreleased") for the adjudicated path-matching contract described below;
`CFG-001` pins its own production-Go oracle at `fc9eddc0`, the commit that
introduces the pure `ResolvePaths` seam; `RUST-007`'s config/platform oracle is
advanced from `caadd5e` to `31afe33c` because the old claim was never verified
and four of its seven production sources already differed from `caadd5e`.
At that audit, `STORE-004`'s `manifestkeygen` revision advanced to `1bc61ea6`
because it then bound the whole `internal/` tree; its 16 vectors were unchanged.
Other rows, including `PAIRING-001`, retained the baseline oracle at that time.

The `STORE-004` manifest-key fixture originally bound every `internal/` Go
source, including unrelated intake code. The #1126/#1127 change narrows
that fixture's enforced source tree to `internal/vault`, `internal/crypto`,
`internal/config`, and `internal/fsutil`. The immutable, main-reachable
`3232e31f` oracle revision stays pinned; only provenance digests change,
not the 16 behavior vectors.

Oracle commits are no longer trusted as labels. Every fixture generator now
resolves its claimed commit through git and compares the working tree against
that commit's immutable blobs, refusing to emit a fixture whose provenance does
not hold. Most use the shared check in
`scripts/rust-port/internal/provenance`; `configprofilegen`, `manifestkeygen`
and `storemetagen` predate it and verify through `git archive` / `git ls-tree`
against the same pinned commit, which is equivalent and was left alone rather
than churned. (`auditverify`, `caserun`, `cryptoverify`, `devicelistdriver`,
`diffharness`, `nativekeyringprobe` and `storereopen` are harnesses and probes,
not fixture generators, and pin no oracle.)

The last four were bound at `c4552c43`, and binding them proved the concern was
not theoretical. `portgen` was the worst of them: `--check` read the oracle
block back out of the very fixture it was meant to certify and stamped it into
the regenerated document, so the claim was **unfalsifiable** — any commit
string, right or wrong, survived every check forever. Its fixture claimed
`caadd5e` while `cmd/` had moved on to `a518124f`.

`coregen`, `quotagen` and `sessionquotagen` validated the commit as a label.
Four of their fixtures claimed the frozen `caadd5e` (v0.22.1) baseline while
pinning code that commit does not contain:

| fixture | claimed | actual | why the label was wrong |
|---|---|---|---|
| `quota-contract.json` | `caadd5e` | `8913d64e` | `internal/policy/ratelimit_transition.go` **did not exist** at `caadd5e`; it was extracted later |
| `session/contract.json`, `quotas/contract.json` | `caadd5e` | `8913d64e` | same, plus `internal/policy/ratelimit.go` changed after the baseline |
| `redact-contract.json` | `caadd5e` | `ba4dc068` | four of seven redact/masking sources changed after the baseline |
| `password-totp-contract.json` | `caadd5e` | `8913d64e` | both crypto sources changed after the baseline |

Regenerating all five changed **only the oracle blocks — not one case vector,
and not one of the command tree's 135 commands**, so the
fixtures were factually correct and merely mislabelled. That is the best
possible outcome of the audit and also the reason the defect could survive:
nothing downstream disagreed, because the recorded behaviour was right. What
was unverifiable was the claim about *which revision produced it*.

`coregen` now pins each of its four fixture kinds separately, against the
commit that last touched that kind's own sources. A single shared pin had made
a change in any one package silently re-label three unrelated fixtures, and it
is why two of its fixtures kept an accurate label (`error`, `secret_ref` — both
still legitimately at `caadd5e`) while two did not. The Rust side now asserts
the oracle block for those rows too; previously `error_contract.rs`,
`secret_ref_contract.rs` and `redact_contract.rs` read the fixtures without
checking which oracle they carried.

## POLICY-001 path-matching adjudication

The previously claimed `POLICY-001` `PASS` was disproven: the generator claimed
the pinned oracle while hashing and executing the working tree, and it rewrote
paths to native separators for Go only, so Go and Rust received different
inputs on Windows. Rust additionally never implemented `~/` expansion, and no
fixture case covered `~`, so that divergence was invisible. The adjudicated
contract is:

- Path matching is defined over slash-separated logical paths and is
  OS-independent on both sides. The native separator is interpreted once, at
  the runtime boundary (`policy.ToLogicalPath`), not inside the matcher. A
  slash-written rule previously stopped matching on Windows, which failed open
  for deny rules.
- The bare directory-prefix branch applies only to wholly literal patterns. Its
  old guard tested `*` alone, so `fixture/?` was simultaneously a glob and a
  literal directory name.
- `~/` expands from an explicit `EvalContext.HomeDir`; the matcher performs no
  runtime discovery, keeping the row inside its declared scope.
- `Policy.Validate` rejects syntactically invalid globs, which previously never
  matched anything and so silently stopped denying.
- Patterns collapse duplicate separators, so a UNC-shaped deny rule still
  denies after the value cleaner collapses them.

Neither the historical nor the current matcher was assumed safer: the two move
allow and deny outcomes in opposite directions. Every counterexample is
retained in `path_cases`, each carrying both the allow-rule outcome and the
outcome under a higher-priority deny rule with an allow fallback.

| ID | Seam | Fixture / input | Go oracle | Expected contract | Rust evidence | Platforms | Compare | Status |
|---|---|---|---|---|---|---|---|---|
| CLI-001 | Version | `version`, output variants, extra args, `--version` | Go binary | exact exit/stdout/stderr and schema | `symvault-cli/tests/version.rs` + staged differential suite | macOS/Linux/Windows | bytes | PASS |
| CLI-002 | Command tree | every visible/hidden command | `cmd.NewRootCmd()` generator | paths, aliases, groups, arity, annotations | inventory drift test | all | semantic + bytes | TODO — `scripts/rust-port/cmd/cligap` measures the surface against the pinned tree without an oracle binary: at `1d534410` 46 of 134 oracle paths are unreachable and 3 cobra aliases (`get show`, `get cat`, `list ls`) are rejected; report and method in [`cli-gap-inventory.md`](cli-gap-inventory.md). Surface reachability is not behavioural parity and does not promote this row, and hidden commands stay unmeasured. |
| CLI-003 | Flags | local/inherited flags and defaults | Cobra generator | names, shorthands, types, defaults, required/conflicts | inventory drift test | all | semantic | TODO — the 2026-09-20 measurement finds exactly 9 oracle flags missing on reachable commands (`import --quarantine`; `mcp --bind`, `--port`, `--tls-ca`, `--tls-cert`, `--tls-key`; `run --broker`, `--broker-passthrough`, `--broker-strict`), all belonging to unported slices. Defaults, shorthands, conflicts and inherited-flag resolution are not measured and do not promote this row. |
| CLI-004 | Help/completions/manpages | all commands, four shells, man tree | Go generators | stable content and executable completions | snapshots + shell checks | all | bytes/declared normalization | TODO |
| CLI-005 | Error taxonomy | invalid args/config/auth/not-found | Go binary | exit codes 0–10, stream placement, no secret leakage | differential cases | all | bytes | TODO |
| CLI-006 | Output modes | text/JSON/YAML/NDJSON capable commands | Go binary | field names, ordering, omission, newline behavior | differential cases | all | bytes/semantic by row | TODO |
| CLI-007 | Signals | long-running watch/MCP/broker | Go binary | cancellation, flushing, process-tree cleanup, exit 130 where defined | signal harness | macOS/Linux/Windows equivalent | semantic | TODO |
| CFG-001 | Defaults | empty HOME/XDG | `internal/config.ResolvePaths` over an explicit `PathEnvironment`; discovery stays in `NewPathResolver` and is out of scope | XDG defaults, legacy fallback, the three install states, set-but-empty XDG values, and the `SYMVAULT_VAULT` override including tilde expansion and blank handling | 14 Go-generated cases in `testdata/port/config/paths.json`, consumed by `crates/symvault-core/tests/config_paths_contract.rs`; provenance verified against git blobs by `configpathgen`; wired into `port-contract` as `cfg-fixtures-check` | all | semantic | PASS at `47c03d82`: `path_resolution_matches_go_oracle` ran natively in run 34974354195 on ubuntu (`Rust`, nextest), macos-latest and windows-latest (`Rust native`, `Test including doctests`), plus the Go oracle check in `Rust port contract`. The Windows job caught a real defect on the way: a `SYMVAULT_VAULT` override without a tilde is passed through verbatim while joined fields come back native, so the contract and both implementations compare in slash-separated logical form. The native separator rendering is deliberately out of scope for this row |
| CFG-002 | Precedence | defaults, tier presets and explicitly present YAML fields | `internal/config.Load` plus `config.ApplyTierPreset` | the precedence ladder within config loading: built-in defaults, then the tier preset when `tier` is present, then every explicitly present field — so an explicitly written permission overrides the tier in both directions, and presence rather than value is what decides. Permissions are compared in effective terms, since Go models them as `*bool` and every consumer resolves them as `p != nil && *p`. **Flag and environment overrides are out of scope**: they are not implemented as a precedence seam but scattered per command in `cmd/` and per subsystem, so the row was narrowed to the layering the loader actually performs; the legacy-versus-XDG file choice is `CFG-001` | 23 Go-generated cases in `testdata/port/config/precedence.json`, consumed by `crates/symvault-core/tests/config_precedence_contract.rs`; provenance verified against git blobs by `configprecedencegen`; wired into `port-contract` as `cfg-precedence-fixtures-check` | all | semantic | PASS at `adecf212`: `precedence_matches_go_oracle` and `a_tier_applies_its_preset` ran natively in run 34997271326 on ubuntu (`Rust`, nextest), macos-latest and windows-latest (`Rust native`), plus the Go oracle check in `Rust port contract`. Two real divergences were fixed on the way: Rust recorded the tier name without applying its preset, so a `tier: standard` agent did not require approval and a `tier: admin` agent could not write or run commands; and a present-but-null list was rejected where Go reads it as an explicitly empty list, verified across all five list fields |
| CFG-003 | Config bytes | canonical/unknown/corrupt YAML | `internal/config.Load` / `(*Config).SaveTo` | acceptance of canonical, unknown and corrupt input; the resolved config for accepted input; the warnings the loader raises; the writer's canonical output reloading to the same snapshot; and the 0700/0600 modes the writer leaves behind. Error text is excluded — yaml.v3 and serde_yaml_ng word failures differently — but warning text is pinned, because those are ours on both sides. `vaultDir` is excluded as environment-derived and belongs to CFG-001 | 22 Go-generated cases in `testdata/port/config/bytes.json`, consumed by `crates/symvault-core/tests/config_bytes_contract.rs`; provenance verified against git blobs by `configbytesgen`; wired into `port-contract` as `cfg-bytes-fixtures-check` | all; mode assertions unix-only, recorded as such rather than asserted on Windows | bytes + semantic | **PASS** at `aa21ec4e`: the two implementations agree on all 22 inputs and the divergence set is empty and asserted exactly. The staged migration is complete. Step one (`addce896`) brought Rust to Go's YAML 1.1 booleans and its treatment of an explicit `null`, and put warn-and-accept on both sides for a non-positive session duration and a multi-document stream. Step two (`aa21ec4e`) flipped those two to rejection in both implementations at once, ending a silent discard in which the operator's value was replaced by the default without a word. Because Go's zero value cannot tell an absent key from an explicit `0s`, the loader now tracks which top-level keys the document carries and rejects only what the operator wrote; an absent key still takes the default. Breaking for hand-edited config files containing either input — the writer has never emitted them. Decision paper: `cfg-003-acceptance-adjudication.md`; consumer handoff: `consumer-handoff-config-20260915.md` |
| CRYPTO-001 | X25519 identity | fixed safe test identities | Go crypto | public key and fingerprint parity | cross-language vectors | all | bytes | PASS |
| CRYPTO-002 | age entry encryption | deterministic plaintext corpus | Go crypto | mutual Go↔Rust decryption; recipient behavior | differential vectors | all/iOS | cross-decrypt | PASS |
| CRYPTO-003 | passphrase envelopes | legacy scrypt and current argon2id fixtures | Go crypto | mutual decrypt, parameter parsing, migration flags | fixture/property tests | all/iOS | bytes + cross-decrypt | PASS |
| CRYPTO-004 | re-encryption | multi-recipient add/remove corpus | Go vault | all retained recipients decrypt; removed recipients fail | end-to-end fixtures | all | semantic | PASS |
| CRYPTO-005 | password/TOTP | fixed RNG/clock vectors | Go generators | character policy, TOTP code/period/expiry | unit/property tests | all | bytes | PASS |
| POLICY-001 | policy/tier evaluation | versioned rule cases with explicit `EvalContext` fields (agent/path/tags/working dir/env/action/tool/UTC clock/secret count), plus read-only/standard/admin tier inputs; no YAML/filesystem/runtime discovery or YAML field-presence precedence | `internal/policy.(*Policy).Validate`, `internal/policy.NewEngine`, `(*policy.Engine).Evaluate`, `(*policy.TimeRange).Contains`, `internal/config.GetPreset`, `internal/config.ApplyTierPreset` | deterministic validation, priority ordering, condition matching, wrap-around time ranges, default deny/result shape, and exact tier preset values/copy behavior; `Engine.Evaluate` is exercised only for pure branches with an explicit `EvalContext`, with `RateLimiter` and `AuditLogFunc` side effects excluded; rate-limit behavior belongs to `QUOTA-001`/`QUOTA-002`. Policy loading and runtime discovery remain outside this row; YAML field-presence/config override remains `CFG-002`/`RUST-007`; approval queues and MCP tool filtering/call-time enforcement belong to `RUST-010` through `MCP-002`/`MCP-003`, including later policy audit/enforcement integration. | Go-generated JSON vectors consumed by `symvault-core` unit/property tests; fixed-context differential cases; provenance and source/generator digest drift gate in `policygen`; `cargo +nightly miri test -p symvault-core`; provenance is now verified against git blobs rather than a label, Go and Rust consume byte-identical verbatim inputs, and the 21 `path_cases` retain every adjudicated counterexample in both allow and deny polarity | macOS/Linux/Windows/FreeBSD | semantic + bytes for serialized results | PASS at `042d8aa4`: `native_closeout.py portable` regenerated the fixture natively on ubuntu-latest, macos-latest and windows-latest and each produced the identical SHA-256 `8824b24c3d5dfda4158074806f388b4286e366dcd17dac17525370f0148a22f4`, matching the committed fixture (`policy_fixture_matches_committed: true` on all three) — the contract is OS-independent, so this is the input-equality proof; the Windows job of `rust-pairing.yml` previously failed this row at `424d63c5` and now succeeds (run 34948850562). `Rust port contract`, `Rust` (incl. pinned-nightly Miri) and `Rust native` on macos-latest and windows-latest all succeeded in run 34948848009. FreeBSD remains unexecuted, as for every row in this matrix |
| QUOTA-001 | pure quota/rate-limit bucket transitions | Before any fixture generation or porting, add a narrow production-compatible Go seam: pure `internal/policy.TransitionRateLimit(state RateLimitState, limits RateLimitLimits, now time.Time, event RateLimitEvent) -> (nextState RateLimitState, result RateLimitResult)` over language-neutral in-memory state carrying `tokens`, `capacity`, `refillRate`, `lastRefill`, `dailyCount`, `dailyWindowStart`, and `maxPerDay`; cover only explicit `set_limits` and `allow` events, refill, daily-window rollover, and limit decisions; no registry, filesystem, or process locking | the required `internal/policy.TransitionRateLimit` seam | deterministic single-bucket state transitions from explicit state/time/limits; `AgentRateLimiter` unknown-agent, `HasLimits`, per-agent isolation, and `Cleanup` are excluded and specified by the `QUOTA-002` wrapper fixture layer | Go-generated JSON transition vectors from the production helper plus Rust property tests over explicit clock/state; differential evidence in `symvault-core`; no `PASS` until fixtures/tests run in CI | macOS/Linux/Windows/FreeBSD | semantic state-transition comparison | PASS |
| QUOTA-002 | persistent quotas and registry wrapper | versioned `.quotas.json` layout (`{"counters":{...}}`), vault-directory/file modes (0700/0600), `New`/`Increment`/`Check`/`Reset`/`Close`, closed/malformed/I/O errors, same-process and cross-process concurrency; Unix `flock` and Windows `LockFileEx`; plus a separate public-wrapper fixture layer for `AgentRateLimiter` unknown-agent behavior, `HasLimits`, per-agent isolation, and `Cleanup`; no copied transition logic or private-field mutation | `internal/quotas.New`, `(*quotas.QuotaCounter).Increment`, `Check`, `Reset`, `Close`, plus `internal/policy.NewAgentRateLimiter`, `(*policy.AgentRateLimiter).Allow`, `SetLimits`, `HasLimits`, and `Cleanup` | exact `.quotas.json` bytes/schema, modes, durable reset/increment/check behavior, error outcomes after close/corrupt/I/O failures, process-safe Unix/Windows locking and concurrent updates, and executable registry-wrapper semantics; pure bucket transitions remain `QUOTA-001` | Go-generated fixtures + Rust wrapper/error/process tests; native macOS Unix-lock evidence; **native Windows `LockFileEx` runtime evidence exists**: `persisted_layout_and_modes`, `concurrent_updates_are_not_lost` (`crates/symvault-core/src/persistent_quota.rs`) and `quota_cross_process_updates_are_not_lost` (`crates/symvault-platform/tests/quota_platform_contract.rs`) are not `cfg(unix)`-gated and run through `NativeQuotaPlatform`'s `#[cfg(windows)]` branch; `ci.yml`'s `rust-native (windows-latest)` job executes `cargo test --workspace --all-features --locked` natively and succeeded at `829ef3d1` in run 34882426502, as did `Rust` on ubuntu-latest via `make rust-gates` -> `rust-test`; the Go fixture provenance check is now CI-enforced as well, after the host stamp that made it unverifiable off-host was removed and `rust-007-fixtures-check` was added to `port-contract` | Linux/macOS/Windows native CI | bytes + metadata + side effects + semantic | PASS |
| STORE-001 | vault layout | fresh and legacy vault trees | Go init/open | paths, files, modes, migration and no data loss | filesystem manifest; dotted/pseudonymized writer paths proven by `TestEntryWriterGoRustLiveAcceptance` (dotted `nested.name/service.v1`, both writers, both pseudonymize states), running natively on ubuntu/macOS/Windows via `.github/workflows/rust-store.yml` ("Rust storage differential", verified green in runs 34868517339 and 34869405625) and `ci.yml` (#1019) | all | metadata + hashes | PASS |
| STORE-002 | entry model | minimal/full/nested/large values | Go loader | YAML shape, metadata, ordering, limits, type inference | eight persisted metadata vectors, `persisted_rust_metadata_matches_all_go_writer_vectors`; `storemetagen -check` wired into `port-contract` / CI and verified; evaluated: existing fixture-based Rust test against Go PrepareEntryForWrite output plus CI enforcement matches PASS rigor of sibling rows; live Go/Rust entry writer interoperability proven in `TestEntryWriterGoRustLiveAcceptance` | all/iOS | bytes + semantic | PASS |
| STORE-003 | safe filesystem | symlink/traversal/read-only/partial write | Go fs/vault | fail closed, atomicity, modes, cleanup | adversarial tests, including `atomic_create_holds_parent_capability_across_ancestor_replacement` and `replacement_is_atomic_and_never_follows_symlink_targets` for the shared primitives, and `write_new_entry_and_write_entry_at_reject_symlinked_entry_parent` exercising `write_new_entry`/`write_entry_at` themselves end to end against a symlink-swapped `entries/` subdirectory (mutation-checked: fails without `rooted::directory`'s `OFlags::NOFOLLOW`, passes with it); invalid-recipient case in `TestEntryWriterGoRustLiveAcceptance` proves zero filesystem mutation on rejection; running natively in CI on all matrix platforms | all | side effects | PASS |
| STORE-004 | manifests | valid/tampered/out-of-band entries | Go manifest code | exact verification and diagnostics | fixture suite; configured-recipient decrypt access and metadata-applied-once semantics proven by `TestEntryWriterGoRustLiveAcceptance` (second-recipient decrypt, single- vs multi-recipient exclusion, `Version == 1`), CI-enforced natively via `rust-store.yml`; publication-failure injection (`publication::tests::injected_*`) covered; manifest map-key parity covered by `manifestkeygen`/`crates/symvault-store/tests/manifest_keys.rs`; cross-process manifest writers recovery test passing | all/iOS | bytes + semantic | PASS |
| STORE-005 | encrypted search index | build/load/stale/corrupt/concurrent | Go vault | no plaintext on disk; matching and invalidation parity | live Go/Rust acceptance (entry writer, manifest sequence, encrypted search index, plus `TestEncryptedIndexBuildEmptyGoRust`) runs natively on ubuntu/macOS/Windows via `rust-store.yml` (confirmed passing across all three OSes). Concurrent-index-ownership/eviction line of work (`SearchIndexStore`, process-wide binding, bounded eviction, load/invalidate serialization) and sibling cross-process-manifest line (`migration/rust-watch-store-cross-process-repair-r2-20260914`) both integrated into this branch and verified | all | side effects + semantic | PASS |
| AUDIT-001 | HMAC chain | fixed key/clock/event corpus | Go audit | canonical JSON, HMAC chain, `kid`, reset detection | byte vectors; local macOS differential twice clean; native 3-OS matrix verified passing in `.github/workflows/rust-audit.yml` (run 34877175625 and 34840987521 on ubuntu/macOS/Windows); non-CI targets remain blocked until native runners execute | all | bytes | PASS |
| AUDIT-002 | key rotation/export | pre/post-rotation logs | Go audit | archive naming, verification, redaction, filters | fixture suite; local macOS differential twice clean; native 3-OS matrix verified passing in `.github/workflows/rust-audit.yml` (run 34877175625 and 34840987521 on ubuntu/macOS/Windows); non-CI targets remain blocked until native runners execute | all | bytes + metadata | PASS |
| SESSION-001 | cache | save/load/touch/expiry/revoke | Go session | idle/max TTL and non-refreshing probes | generated session fixture + `symvault-core` injected MemoryKeyring/session tests | all | semantic | in_progress |
| SESSION-002 | OS keyring | memory backend + native smoke | Go session | service/account names, binary payload, unavailable behavior | injected tests plus explicit `native_keyring_binary_roundtrip_and_delete`: macOS arm64 passed a generated test-only service/account through missing/set/binary get/update/delete/idempotent delete; no pre-existing keychain item or user vault data was read or changed. The keychain `user` domain does resolve through `$HOME`, so `native_closeout.py keyring`'s private HOME does redirect it to a disposable keychain — but only once `Library/Preferences` exists there, which is why every earlier probe reported `OS keyring unavailable` while claiming successful provisioning; the provisioning now verifies itself and the probe runs green on macos-latest (run 34957599911), exercising the Go `NewOSKeyring` backend and the Rust `native_keyring` test against the same provisioned keychain. Those are two independent round-trips, not a cross-implementation parity check: the probe's `rust_parity` is a hardcoded `false`, and no test yet has one implementation read what the other wrote. Linux and Windows Go-write/Rust-read-update/Go-read-delete interoperability passed in run `35246939742` at PR merge `ffe087d1` (head `6c0bbcca`). macOS cross-language access hung on the framework adapter; a Go-compatible bounded security-helper adapter is under validation | macOS native diagnostic; isolated and other native OS pending | semantic **The portable half of this row now has a contract.** `keyringkeygen` freezes the key-addressing rules at `7743fd21` into `testdata/port/session/keyring-keys.json` (12 split cases, 7 in-memory backend scripts, 2 pinned divergences), consumed by `crates/symvault-core/tests/keyring_keys_contract.rs` and wired into `port-contract` as `keyring-key-fixtures-check`, so it runs on every OS -- the native keychain stays a macOS-gated diagnostic, but the addressing rules are platform-independent and were previously unverified anywhere. The split is now one function per side (`session.SplitKeyringKey`, `symvault_core::session::split_keyring_key`). Three divergences were found: a key without the `service|account` separator was silently stored by Go under an empty service where every such key collides, while Rust refused it (**fixed**: Go refuses it before touching the keychain); deleting an absent entry returned an error from Go's OS backend against its own documented idempotence, the in-memory backend and Rust, with every caller carrying a compensating branch (**fixed**); and Go's in-memory backend was not opaque storage at all -- its session-account `Get` parsed the value and **deleted the entry** when the parse failed, destroying a value `Set` had just accepted and reporting it as never having existed, on the path that activates precisely when the OS keyring starts failing (**fixed at `29c5e5ef`** after adjudication in `session-002-memory-backend-adjudication.md`: the backend is a plain store, the duplicated TTL rule and the dead second encryption path are gone, `Manager.LoadPassphrase` owns all of it as it already did, and the five tests that pinned the removed logic were replaced at the layer that now owns the property rather than dropped). The divergence set is empty and asserted exactly. | in_progress |
| SESSION-003 | Touch ID | available/unavailable/cancel/failure | Go Darwin bridge | prompts, fallback, no passphrase exposure | macOS LocalAuthentication JXA adapter; non-interactive availability and decision-only script tests; auth prompt/hardware smoke not run | macOS local; hardware/signed-app CI pending | semantic | in_progress |
| PLATFORM-001 | clipboard/autotype | fake backend + native smoke | Go adapters | permission, clear timer, field routing, cancellation | injected trait tests plus macOS `pbcopy`/JXA adapters and non-interactive capability probes; clipboard/autotype side effects not run | macOS local; native permission/UI smoke pending | semantic | in_progress |
| PLATFORM-002 | secure UI/notifications/daemon | injected platform commands | Go adapters | backend selection, escaping, timeouts, lifecycle | injected trait tests plus macOS JXA secure UI/notification and launchd plist adapters; ignored native `native_daemon_private_home_lifecycle_attempt` installed, queried, and removed a temporary LaunchAgent successfully on macOS arm64; no user LaunchAgents path was touched. GUI secure input/notification delivery remains unexecuted | macOS native daemon diagnostic; GUI permission smoke pending | semantic | in_progress |
| GIT-001 | init/commit/log | isolated local repositories | Go `go-git` | refs, messages, dirty state, gitignore behavior | `testdata/port/sync/sync.json` generated by `syncgen`; `symvault-sync/tests/contracts.rs` | all | semantic | PASS |
| GIT-002 | push/pull/auth | local bare remote and failures | Go `go-git` | divergent-pull error/rollback projection and shared offline classifier are proven. Rust now bounds every Git subprocess, disables terminal prompts while preserving configured askpass helpers, cleans up timed-out process groups on Unix (Windows now uses a suspended child assigned to a kill-on-close Job Object before resuming), and applies the Go push/pull error-precedence rules; focused regressions cover auth/SSH precedence, mixed connectivity/auth text, prompt suppression, and timeout cleanup. The runner/classifier probes are Rust-side unit regressions; the source-bound Go fixture `testdata/port/sync/git-io.json` pins Oracle `28fd35315cf4989821a96bb08279c999c693e8d9` and includes five productive projections (offline, HTTP authentication, SSH known_hosts precedence, inherited askpass/terminal prompt, and timeout/descendant cleanup), replayed by the real Rust repository path. The fixture check is `make git-io-differential`; native SSH/askpass/timeout process evidence remains separately scoped to the productive Rust tests. | `testdata/port/sync/sync.json` generated by `syncgen`; exact divergent-pull projection in `symvault-sync/tests/git_io_gaps.rs`; plus `scripts/rust-port/cmd/gitofflinegen` and `scripts/rust-port/cmd/gitwinnergen`; the former pins `IsOfflineError` over 30 messages (22 offline, 8 not), its 18-marker order, and `PushError` rendering, while the latter covers all 14 winner cases. Replayed by `make git-offline-differential`, `make git-winner-differential`, and `make sync-io-differential`. | macOS arm64 (executed); Linux/Windows native runtime unexecuted | semantic | **in_progress** at candidate `cd30dcd5d4816944152cb914d7186b7aeb62a2f3`: portable GIT-002 independently APPROVED by review; native Linux/Windows auth/process evidence remains open. |
| GIT-003 | reconciliation | divergent versions/conflicts | Go sync | lossless deterministic conflict-copy projection **and** the version-winner rule, which is now covered rather than pending: all three tiers (version, then `Updated`, then the canonical-JSON tiebreak), order independence as a property, and the one branch that is deliberately order-DEPENDENT — an empty `path` short-circuits to the first argument | `testdata/port/sync/sync.json` generated by `syncgen`; lossless projection and order-independence tests in `symvault-sync/tests/contracts.rs`; plus `scripts/rust-port/cmd/gitwinnergen`, which calls `WinnerByVersion` directly for 12 cases (3 version, 3 updated, 6 tiebreak), runs every pair in both argument orders, and records the oracle's own compact marshaled bytes for each operand. Replayed by `crates/symvault-sync/tests/version_winner_contract.rs` (6 tests) against `winner.rs` via `make git-winner-differential`; 14 cases after review | all | hashes + semantic | **in_progress** at candidate `cd30dcd5d4816944152cb914d7186b7aeb62a2f3`: portable GIT-003 independently APPROVED by review; the version-winner gap is closed, while the remaining native/dependent RUST-008 gates keep this row open. The tiebreak decides by comparing serialized bytes, so the differential asserts Rust's canonical JSON is byte-identical to the oracle's rather than only checking which side won — an encoding drift would silently change every tie. Mutation-checked 9/10; the single survivor (`<=` weakened to `<`) is equivalent, and that is recorded as an executable test rather than asserted in prose: the two differ only when the encodings are equal, which means the values are equal, so the swapped pair is unobservable. One real corpus gap was found and closed by the mutations — no case had a fractional second, so comparing the timestamps as strings survived. Go trims trailing zeros, so `07.5Z` sorts before `07Z` while being the later instant; that pair is now in the corpus and Rust parses to instants. **Portable scope independently APPROVED at candidate `cd30dcd5d4816944152cb914d7186b7aeb62a2f3`;** the row remains `in_progress` because no native 3-OS run has executed this row and RUST-008's remaining native/dependent gates are open. **Independent review (2026-09-16) found real defects; all are fixed and each is now covered by a corpus case that fails without its fix, verified by reverting it.** Go's `json.Marshal` escapes `<`, `>`, `&` and U+2028/29 and compacts a `RawMessage`; `serde_json` does neither. Both are now routed through the new `symvault-gojson` crate. **For this row the defect was the most serious found: the canonical-JSON tiebreak picked the opposite winner.** `<` escapes to `\u003c`, which moves the byte from 0x3C — below `=` — to 0x5C, above it. With a user-controlled tag containing `<`, Go and Rust selected *different entries* for the same path, a silent data divergence in sync reconciliation. The test written specifically to catch encoding drift never fired because no operand contained an escapable character. Two such cases are now in the corpus and both the implementation and the differential encode through `symvault-gojson`. |
| IO-001 | importers | CSV/browser/Bitwarden/1PUX/pass/CXF corpora | Go importer | accepted data, warnings, mapping, limits and malformed input; production pass process adapter exercised with isolated synthetic GPG | Existing sync corpus plus source-bound `csv.json` (27 import, 20 TOTP and 7 profile cases), `onepass.json` (10 archive/process cases), `cxf.json` (19 cases), and Rust adapter tests; CSV/onepass/CXF differential targets | macOS local; broader native corpus pending | semantic | **in_progress**: earlier PASS covered a narrow subset; CXF integrated; Bitwarden casefold/null and invalid UTF-8 slices, plus real GPG compatibility using disposable keys, are locally covered. Complete type/case handling, invalid-byte API representation and import CLI remain open. The generic 100 MiB parser cap has been removed |
| IO-002 | export/backup/restore | fixed vault | Go commands | export bytes plus archive members, modes, restore and traversal defense; compressed container bytes excluded | `sync.json` archive corpus plus source-bound `export.json`: five JSON/CSV cases for nested values, escaping, column mapping and attachment notices; failing output/notice sinks | macOS local; native gates pending for expanded export corpus | bytes + metadata | **in_progress**: prior PASS covered a small flat-data sample. Expanded export behavior is verified locally; Go float64 number formatting is covered by the source-bound CSV export fixture; mapping collisions and complete CLI remain open |
| IO-003 | intake/watch | isolated directory events | Go intake | portable staging/dedupe/debounce evidence only; native watcher behavior remains unproven because the pinned Go production Watcher is polling-only | Go-generated portable case plus Rust `scan_at`/process tests; no native event backend exists in the pinned oracle | macOS portable evidence only; Linux/Windows unexecuted | semantic | in_progress |
| MCP-001 | initialize | 22 raw-line cases: negotiation across every supported version plus one below and one above the set, absent/empty `protocolVersion`, absent params, string/numeric/explicit-null IDs, the three silent notifications, `ping`, and the malformed/wrong-envelope/missing-method frames | `internal/mcp/server.ProtocolHandler.HandleMessage` and `internal/mcp/transport.StdioTransport` driven end-to-end by `scripts/rust-port/cmd/mcpinitgen`, which captures the verbatim stdout byte stream rather than reimplementing the handler | the emitted byte stream, including JSON key order, which follows the oracle's Go struct field order and is therefore part of the contract; negotiated version; capabilities; `serverInfo`; the `instructions` blob; error codes and the oracle's own `data` literals; and stdout silence for notifications. Excluded and masked to `<runtime-error-text>`: the `data` of `-32700`/`-32602`, which is `encoding/json`'s own wording — the four affected cases still assert a non-empty diagnostic. The oracle's own literals (`jsonrpc must be 2.0`, `method is required`) are explicitly *not* masked | 22 Go-generated cases in `testdata/port/mcp/initialize.json`, provenance-verified against git blobs by `mcpinitgen` and wired into `port-contract` as `mcp-init-fixtures-check`; replayed by `crates/symvault-mcp/tests/initialize_contract.rs` (9 tests) against the new `symvault-mcp` crate via `make mcp-init-differential`; the corpus grew from 22 to 30 cases after independent review | all | bytes + semantic | **in_progress** at `448c8ceb`: locally complete and green — `make port-contract` (exit 0, `PASS MCP-001 initialize fixture (22 cases)`), `cargo test --workspace --all-features --locked` (300 tests, 0 failures), `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` and `cargo fmt --all --check` all exit 0 on go1.26.6/rustc 1.98.0. The differential is mutation-checked: 12 mutations, 12 caught, including dropping a capability, flipping `listChanged`, swapping two result fields, and answering a notification. Two real defects were found by it and fixed rather than accommodated — `serde_json::json!` sorts keys and so broke the oracle's field order, and `Option<Box<RawValue>>` collapsed an explicit `"id": null` into an absent ID, which would have turned a request into a notification and hung the client. **Not `PASS`:** no native 3-OS run has executed this row yet. The contract is pure string/JSON logic with no OS interaction, so no divergence is expected, but expectation is not evidence; promotion waits on the bundled PR's `Rust port contract` and `Rust` jobs. **Independent review (2026-09-16) found real defects; all are fixed and each is now covered by a corpus case that fails without its fix, verified by reverting it.** Go's `json.Marshal` escapes `<`, `>`, `&` and U+2028/29 and compacts a `RawMessage`; `serde_json` does neither. Both are now routed through the new `symvault-gojson` crate. For this row that fixed four separate parity defects the original corpus did not reach: `"params": null` and `"protocolVersion": null` were rejected where the oracle treats `null` as a no-op and completes the handshake (a conforming JSON-RPC client was refused); a malformed `clientInfo` was *accepted* where the oracle returns `-32602`, which is the wrong direction for a fail-closed port; `encoding/json`'s case-insensitive field fallback was missing, so `PROTOCOLVERSION` silently negotiated a different version than the oracle; and the emitted frame escaped nothing, putting attacker-chosen `<`/`>`/`&` from the method name and the echoed id onto the same stdout stream the client parses for framing. |
| MCP-002 | tool surface | agent tiers and runtime capabilities | Go `tools/list` | all 35 definitions, schema, order, annotations, availability | `make mcp-list-differential`: 35 schemas and 11 Go-generated profile/runtime cases, immutable source/generator freshness, plus negative control | all (current slice locally checked on macOS arm64) | bytes/semantic | **in_progress**: native catalog/list dispatch with injected capabilities; runtime discovery, full profile matrix and call-time authorization remain open |
| MCP-003 | tool calls | success/invalid/scope/backend corpus | Go server | result envelopes, `structuredContent`, errors, redaction | source-bound call fixtures, `make mcp-call-differential`, including connected `secret_unseal` with fail-closed scope and approval checks | all | bytes/semantic | in_progress: many Go tools and complete call-time authorization remain open; local integrated differential passed before native CI |
| MCP-004 | stdio hygiene | 15 hostile-frame cases (unterminated/CRLF/whitespace/NUL/two-objects-per-line/non-object frames/blank lines/interleaved valid and invalid) plus 3 shape-described size probes | the same `internal/mcp` transport and handler as MCP-001, driven by `scripts/rust-port/cmd/mcpstdiogen`, which records the verbatim stdout stream and **measures** the nesting boundary by binary search rather than copying `encoding/json`'s constant | zero stdout pollution (every emitted byte is one newline-terminated JSON-RPC frame, asserted as a property over the whole corpus); frame-for-frame ordering under hostile input; and the real bounds. **Two row-description corrections, both measured:** input is *not* bounded — a 5 MB frame is accepted and answered — and an unterminated final line is *silently discarded* rather than answered, because `ReadString` returns it with `io.EOF` and the loop returns before dispatching. Nesting is the one real bound, at total depth 10000 accepted / 10001 rejected | 15 cases + 3 bounds in `testdata/port/mcp/stdio-hygiene.json`, provenance-verified by `mcpstdiogen`, wired into `port-contract` as `mcp-stdio-fixtures-check`; replayed by `crates/symvault-mcp/tests/stdio_hygiene_contract.rs` (10 tests) via `make mcp-stdio-differential`; 4 bounds after review | all | raw bytes | **in_progress** at `a565f2ea`: locally green (`make port-contract` exit 0, full workspace tests, clippy `-D warnings`, fmt). Mutation-checked 7/7 after closing one real gap the mutations exposed — no case covered bracket characters inside a string literal, so a depth guard that ignored string state would have rejected frames the oracle accepts. **Two adjudicated divergences, both recorded in the fixture and asserted in tests, not prose-only:** duplicate object keys (oracle: silent last-wins; port: `-32700`) and invalid UTF-8 in an id (oracle: accepted and echoed verbatim onto stdout; port: `-32700`). Both are the stricter side; duplicate-key last-wins is the smuggling shape where an auditing intermediary and the server disagree about the request, and SymVault has such a layer. Reversing either is a coordinator decision. The generator re-executes both against the oracle and fails if they stop reproducing. A third difference was matched rather than adjudicated: a literal `null` frame answers `-32600`, because Go's `Unmarshal` treats `null` as a no-op against any target. **Not `PASS`:** no native 3-OS run yet. **Independent review (2026-09-16) found real defects; all are fixed and each is now covered by a corpus case that fails without its fix, verified by reverting it.** Go's `json.Marshal` escapes `<`, `>`, `&` and U+2028/29 and compacts a `RawMessage`; `serde_json` does neither. Both are now routed through the new `symvault-gojson` crate. For this row it closed a coverage hole the earlier 7/7 mutation run had missed — no case exercised backslash escapes in the string-aware depth scanner, so deleting that branch survived — and it removed a name-keyed special case in the bounds replay that had made the recorded probe shape decorative rather than load-bearing. The duplicate-key divergence text is also narrowed to what was measured: it covers duplicated *envelope* fields, while a duplicate inside `params` is `-32602` and one inside a raw id is still accepted last-wins, so the tool-call payload an auditing intermediary would read is explicitly NOT yet covered. |
| HTTP-001 | MCP HTTP/SSE | request/session/replay matrix | Go HTTP server | routes, statuses, headers, SSE framing, shutdown | HTTP transcript tests | all | bytes/semantic | TODO |
| HTTP-002 | bearer/scoped tokens | issue/use/expire/revoke/rotate | Go auth | storage bytes, scopes, TTL, aliases, failure codes | Go 1.26.6 source-bound `token-lookup.json` fixture and `make token-lookup-differential` cover raw-bearer lookup, expiry, revocation, wildcard/exact canonical scopes | all | bytes + semantic | in_progress: HTTP authorization, aliases, issuance, refresh and full failure-code behavior remain open; native CI pending |
| HTTP-003 | OAuth/PKCE/DCR | discovery/register/authorize/token/refresh | Go auth server | endpoints, validation, persistence, single-use rotation | protocol suite | all | transcript + bytes | TODO |
| HTTP-004 | origin/TLS/limits | hostile hosts/origins/bodies/timeouts | Go server | CSRF/host checks, request bounds, TLS behavior | adversarial suite | all | semantic | TODO |
| PAIRING-001 | device pairing handshake | `device pair`/`join`/`accept`/`list`/`add`/`revoke` corpus, transport-independent `PairingFile`/`JoinResponse` JSON artifacts (git remote, synced-folder, and `<token>-joined.json` alias) | `internal/pairing.{PairingFile,JoinResponse}`, `cmd/device.go` (`newDevicePairCmd`, `newDeviceJoinCmd`, `newDeviceAcceptCmd`, `newDeviceListCmd`, `newDeviceAddCmd`, `newDeviceRevokeCmd`) | exact handshake artifact bytes/aliases, new-recipient re-encryption on accept (shares `CRYPTO-004`), revoked device no longer decrypts, expired/malformed/replayed artifacts rejected | `scripts/rust-port/cmd/pairinggen` freezes 147 cases from the pinned oracle into `testdata/port/pairing/contract.json`, replayed in full by `crates/symvault-sync/tests/pairing_contract.rs` against `pairing.rs` (artifact codec, alias lookup, token path-safety, single-use/expiry/cooldown token store), `devices.rs` (`.symvault/devices.json`) and `recipients.rs` (`recipients.txt`), over the shared symlink-hardened atomic IO in `safeio.rs`; `make pairing-differential`, native 3-OS matrix `.github/workflows/rust-pairing.yml` green on ubuntu/macOS/Windows at `12d112d1` in run 34889579439. Mutation-checked: 37/41 on the codec, 15/15 on the registry, 16/18 on recipients, 6/6 on the shared IO; every survivor analysed and shown equivalent. CLI orchestration for `pair`/`join`/`accept`/`list`/`add`/`revoke` implemented in `symvault-cli` with unlock, age identity setup, atomic re-encryption, and git auto-commit; verified via `make device-list-differential` and end-to-end integration tests in `crates/symvault-cli/tests/device_pairing_cli.rs` | all | bytes + semantic | in_progress |
| APPROVAL-001 | local approval CLI, device session tokens, and mTLS client identity | `symvault approval list/decide` over authenticated loopback; `DeviceSessionStore` enrollment/revocation/expiry corpus; generated/custom TLS and CA rotation/revocation cases | `internal/approval.{HandleList,HandleDecide,LocalHTTPHandler}`, `internal/pairing.{NewDeviceSessionStore,(*DeviceSessionStore).{Enroll,Validate,Revoke,List,CleanupExpired}}`, `internal/mcp/serverbootstrap/http.go` (`ValidateMTLSSettings`), `cmd/approval.go` | loopback-only + vault-directory-ownership proof for the local CLI; device-token bearer auth for the remote queue endpoint; hashed-only persisted tokens (no raw bearer on disk); dedicated non-reusable approval-client mTLS identity; missing/malformed/revoked/reused identity and `allow_insecure_bind`+mTLS fail closed; double-decision conflict rejected | source-bound Go device-session fixture plus Rust `device_sessions` store/contract tests, gated by `make device-session-differential`; approval CLI and mTLS fixtures absent | all | bytes + semantic | in_progress: session token persistence, revocation, expiry and legacy migration locally checked; authenticated approval endpoints and identity/TLS cases remain open; native CI pending |
| BROKER-001 | command execution | fake executables and process trees | Go run/broker | env injection, PTY, timeout, cleanup, redaction | process harness | native OS | semantic | TODO |
| BROKER-002 | outbound API/templates | loopback HTTP server | Go broker/template | SSRF policy, headers/body, secret non-exposure | HTTP transcript tests | all | bytes/semantic | TODO |
| FFI-001 | mobile API | every `pkg/mobilebind` function | Go XCFramework | signatures, JSON, bytes, errors | Swift/Rust integration tests | macOS/iOS | bytes/semantic | TODO |
| FFI-002 | extension budget | read/decrypt/list fixture | Go and Rust frameworks | measured RSS and latency on real device | device report | iOS | measured | TODO |
| DIST-001 | targets | current build matrix | Go release | linux/windows/freebsd amd64+arm64; darwin amd64+arm64 | native/cross smoke | all | executable | TODO |
| DIST-002 | archives/packages | release snapshot | GoReleaser | names, members, completions, manpages, MCPB, DEB/RPM/APK | manifest script | all | metadata + hashes | TODO |
| DIST-003 | trust chain | signed prerelease | current workflow | checksums, cosign, SBOM, provenance, macOS signing/notarization | release readback | all | cryptographic | TODO |
| DIST-004 | Homebrew/Scoop/Docker/Nix | isolated installs | current release | install, version, init, persisted paths | ecosystem smoke | relevant OS | semantic | TODO |
| DIST-005 | rollback | Rust-written copied vault | frozen Go fallback | Go opens and mutates safely after rollback | release harness | all | semantic + hashes | TODO |
| VALUE-001 | value gate | representative release builds | measured Go baseline | >=20% size or RSS gain; <=10% p95 regression | paired benchmark JSON | macOS arm64 + CI sample | measured | TODO |

> RUST-006 passed: `make audit-differential` passed twice from clean `target/audit` outputs on macOS, plus the focused Rust audit test (canonicalization, mutation/reorder/reset negatives, rotation, count/age retention, export ordering, filtering, and redaction) and deny-warnings Clippy checks. The fixture is generated through the production Go audit package and is provenance-bound to Go commit `a57f565a`, including both build-tagged keystore implementations. `.github/workflows/rust-audit.yml` ran the same pinned Go/Rust differential on native Ubuntu, macOS, and Windows runners with Go 1.26.6 and Rust 1.98.0 and passed (runs 34840987521 and 34877175625). FreeBSD and other non-CI targets remain explicitly blocked until native runners execute.
>
> RUST-007 local executable evidence: `make config-session-differential` passes: the Go generators produce and check provenance-bound config, session, persistent-quota, and platform fixtures, and Rust consumes them with config/session parity, public quota-wrapper, cross-process lock, malformed/I/O, and fail-closed native-boundary tests. The generated platform fixture explicitly records injected-only native evidence. This does not promote CFG/SESSION/PLATFORM to `PASS`: native OS keyring/UI/daemon/clipboard/autotype evidence remains required.

> Correction (2026-09-14, verified against the workflow files and a real run, not inferred): the claim that the Windows `LockFileEx` path was "only cross-compiled" and that "CI fixture integration remains required" for `QUOTA-002` was **wrong on both counts**, and is the same failure mode as the earlier `rust-store.yml` miss recorded in `CHECKPOINT-20260914T1627Z-store005-merges.md` — concluding from a filename grep rather than from the jobs' actual commands.
> - `ci.yml`'s `rust-native` job (`.github/workflows/ci.yml`, `matrix.os: [macos-latest, windows-latest]`) runs `cargo test --workspace --all-features --locked` natively, and `ci.yml`'s `Rust` job runs `make rust-gates` -> `rust-test` (`cargo nextest run --workspace`) on ubuntu-latest. Both succeeded at `829ef3d1` in run 34882426502.
> - `persisted_layout_and_modes` and `concurrent_updates_are_not_lost` (`crates/symvault-core/src/persistent_quota.rs`) and `quota_cross_process_updates_are_not_lost` (`crates/symvault-platform/tests/quota_platform_contract.rs`) carry no `cfg(unix)` gate; only a permission-mode assertion inside the last one is Unix-only. On a Windows runner they execute `NativeQuotaPlatform`'s `#[cfg(windows)]` `native_lock`/`native_unlock`, including a genuine cross-process exclusive-lock case.
> - The one part that really was missing is the CI enforcement of the Go fixture provenance, and my first attempt at it was wrong. Adding `rust-007-fixtures-check` to `port-contract` failed on the very next native run (34886932152, `Rust port contract`): `FAIL session fixture: session/contract.json is stale`. Cause, verified in the generator rather than guessed: `scripts/rust-port/cmd/sessionquotagen` stamps `GOOS: runtime.GOOS` into the fixture's oracle block (`main.go:257`), and `writeOrCheck` compares the regenerated bytes whole, so a Linux runner produces `"goos": "linux"` against a fixture frozen as `"goos": "darwin"` and can only ever report it stale. `crates/symvault-platform/tests/quota_platform_contract.rs:72` hardcodes the same `darwin` expectation. The dependency has been reverted, with the reason recorded in the `Makefile` next to the target.
> **Resolved the same day.** `goos` described the machine that ran the generator, not the pinned oracle, so it was removed from `sessionquotagen`'s oracle block and from the matching assertion in `crates/symvault-platform/tests/quota_platform_contract.rs`. Before re-wiring, the four RUST-007 fixtures were scanned for any other host-dependent value: `goos` was the only one (the string `windows` that also appears is the filename `internal/quotas/lock_windows.go`). Regenerating changed exactly two lines per fixture — the `goos` line removed and the self-referential `generator_digest` updated — with **zero case vectors touched**. `rust-007-fixtures-check` is a dependency of `port-contract` again, so `PASS config/platform fixtures` and `PASS session/quota fixtures` now run in CI's `Rust port contract` job. **`QUOTA-002` is now `PASS`.** The promotion condition was the first green `port-contract` run carrying this change, and it is met: run 34893584462 at `767835d5`, job `Rust port contract`, success. Confirmed from that job's own log rather than from its name — it shows `sessionquotagen` being invoked and printing `PASS session/quota fixtures`, and `configgen` printing `PASS config/platform fixtures (6 config, 4 platform cases)`. Together with the native Linux/macOS/Windows runtime evidence recorded above, every applicable evidence column for this row is now real and executable in CI.
>
> RUST-005 passed: all five storage contracts (STORE-001..STORE-005) are verified with native 3-OS CI execution (`rust-store.yml` runs 34868517339 and 34869405625; `ci.yml` run 34871842743). Both concurrent-index-ownership and cross-process manifest lines are integrated and passing. Local `make store-differential` and `cargo nextest run -p symvault-store` (74 tests) pass cleanly. Direct successors RUST-006, RUST-007, and RUST-008 are unblocked.
>
> `PAIRING-001` and `APPROVAL-001` are new rows added during the 2026-09-13 Phase 1 register audit for real, tested Go functional areas (device pairing/session tokens and the local approval CLI/mTLS identity, added in PRs #1040 and #1048) that were previously untracked here. Neither has any Rust code, generator, or fixture yet, so both are correctly `TODO`; `PAIRING-001` is now claimed by `RUST-008` and `APPROVAL-001` by `RUST-011` in `work-items.json`, matching their real dependencies below. Real, currently-passing Go-oracle evidence for the negative/failure cases (owner: current sole maintainer, `danieljustus`, until the migration assigns per-slice owners):
> - `PAIRING-001` — `go test ./internal/pairing/... -run 'TestDeviceSessionStore' -count=1 -v` passes 14 cases at HEAD, including `TestDeviceSessionStore_Revoke`, `_RevokeByOtherInstanceTakesEffect` (negative: revoked token stops authenticating from a second process), `_UnknownToken`, `_ExpiredSession`, `_PersistedFileContainsOnlyHashedValues` (negative: raw bearer never touches disk), and `_MigratesLegacyRawTokenKeys`. Dependencies: `CRYPTO-004` (re-encryption on `device accept`) and the storage/IO family under `RUST-008`.
> - `APPROVAL-001` — captured at commit `a518124f` (PR #1048, merged 2026-09-12), which is after the `caadd5e`/`v0.22.1` baseline oracle. `go test ./internal/approval/... ./internal/cli/... ./internal/mcp/serverbootstrap/... ./cmd/... -run 'TestLocalHTTPHandler|TestListRequiresDeviceToken|TestRejectsMalformedAuthorization|TestDoubleDecisionConflict|TestApprovalTLSCertFileRejectsMTLSWithoutLocalClientIdentity|TestValidateMTLSSettingsFailsClosed|TestMTLSRejectsUnauthenticatedClient|TestRuntimeTLSConfigPreservesEffectiveApprovalIdentityWithoutKeyMaterial' -count=1 -v` passes at HEAD, covering: remote/bad-proof rejection on the local endpoint, missing/malformed device-token bearer auth, double-decision conflict, mTLS configured without a distinct local client identity, fail-closed mTLS validation, an unauthenticated client rejected by the TLS listener, and effective-identity metadata that carries no key material. Dependencies: `HTTP-002`/`HTTP-004` (bearer/scoped tokens, TLS/origin behavior) and `PAIRING-001` (device-session tokens), so it is grouped with the `RUST-011` wave, not started before it.
> - No reuse-candidate crate covers either seam; both are Symaira-specific protocol/storage formats, so a hand-ported Rust implementation is expected rather than an upstream dependency.

## Device CLI continuation (2026-09-14)

`PAIRING-001` remains **in_progress**. The candidate now exposes the read-only
`device list --vault <path>` text/JSON path, including unmanaged recipients,
registry ordering, optional last-seen timestamps, RFC3339 second precision,
quiet mode and Go's byte-based key truncation. `make device-list-differential`
builds the real Go CLI from `git archive caadd5e`, executes both binaries in
private HOME/XDG roots, compares successful stdout/stderr bytes and verifies
that neither command changes any manifest entry in its private sandbox.
The CLI cases use `cmd/caserun`, a thin JSON adapter over the established Go
`internal/diff.Run` process-tree runner; raw observations include before/after
manifests, signal and timeout status. Malformed registry cases compare
failure and stream placement only: exact parser diagnostics remain CLI-005.
The initial corpus has 35 live cases and a changed-output rejection control.
Native read-only acceptance passed on macOS arm64, Linux x86_64 and Windows
x86_64 at `49fa5d710f958854b26c998d8f88a690fa3a5ac2` in
[run 34902099288](https://github.com/danieljustus/symaira-vault/actions/runs/34902099288).
All three downloaded reports were checked for the exact SHA, complete unique
case IDs, mutation rejection, unchanged sandbox manifests and absent
signals/timeouts. This evidence covers only this read-only slice, not complete
PAIRING-001 or the remaining configuration/profile and mutation paths.

Review found that Python's Windows `taskkill /T` cannot reliably terminate
descendants after the original parent exits. The CLI cases now reuse the Go
job-object runner. `make device-list-differential` also starts the entire Python
build/test driver through `cmd/devicelistdriver` and the same Go runner, so the
Windows job owns bootstrap toolchain descendants as well as CLI descendants.
Direct unsupervised Python execution on Windows is refused; the actual job
limits are queried before any build. Native adapter/cleanup tests and the
supervised driver passed in the run above. The supervisor independently
validates the complete report and raw observations before returning success.
Reports use fresh timestamped paths and exclusive creation rather than
overwriting prior evidence.

The preserved Go `AddRecipient` separator defect is tracked in
https://github.com/danieljustus/symaira-vault/issues/1055.

## Device CLI mutation & orchestration completion (2026-09-15)

The mutating `device` command family (`pair`, `join`, `accept`, `add`, `revoke`)
is now fully implemented in `symvault-cli` with unlock, age X25519 identity setup,
atomic re-encryption, confirmation gating, and git auto-commit:

1. `device pair`: Generates 32-character base32-hex token via `symvault_sync::pairing::generate_token()`,
   writes `.symvault/pairing/<token>.json` (0600), prints token/key/fingerprint/instructions (unless `--quiet`),
   auto-commits if git repo.
2. `device join`: Handles both `--pairing-file <path>` (1 arg) and `<remote-url> <token>` (2 args) transports,
   validates token, checks uninitialized vault, reads/prompts passphrase (min 12 chars), generates new age identity,
   writes `config.yaml`, saves `identity.age` encrypted with scrypt, writes `recipients.txt`, writes response artifact
   (`<token>-response.json` for file flow or `<token>-joined.json` for git flow), removes invitation artifact,
   and auto-commits.
3. `device accept`: Validates token, unlocks vault, reads response artifact under either canonical alias (`<token>-joined.json`
   or `<token>-response.json`), adds recipient to `recipients.txt`, re-encrypts all entries under `entries/` for updated
   recipients set, rebuilds `manifest.age` if present, removes consumed response artifact, and auto-commits.
4. `device add`: Handles `--pair <token:publickey>`, validates token and age public key, reads passphrase, generates
   identity, writes `config.yaml`, saves `identity.age`, writes `recipients.txt`, writes `.symvault/pairing/<token>-joined.json`,
   and prints setup completion summary to stderr.
5. `device revoke`: Validates initialized vault, unlocks vault, looks up target device in `.symvault/devices.json`, refuses
   self-revocation of the current device, prompts for confirmation unless `--yes`/`-y` is passed, removes device from registry,
   removes recipient from `recipients.txt`, re-encrypts all entries without the revoked recipient, and auto-commits.

Integration tests in `crates/symvault-cli/tests/device_pairing_cli.rs` verify the complete lifecycle:
invitation generation, join response generation, acceptance re-encryption with multi-device mutual decryption,
`device add --pair` and `device revoke` with post-revocation decryption failure, self-revocation refusal, and malformed token rejection.

## Rules

- Every `TODO` receives a deterministic generator or explicit live-test protocol
  before its Rust implementation is considered complete.
- Ciphertext randomness is never normalized into false byte parity. Validate
  mutual decryption, recipient semantics, and parsed format invariants instead.
- Every ignored or normalized field is named here with a reason before use.
- Fixtures use generated test identities and isolated HOME/XDG/keyring backends;
  they never copy data from a real vault.
- Go bugs are not silently preserved or silently fixed. Record a versioned
  contract change first, then update both implementations or the oracle.
