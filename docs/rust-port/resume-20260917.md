# Rust migration continuation — 2026-09-17

This candidate continues PR #1069 from `fca3f894`. Go remains the production
backend; no release, cutover, or Go removal is included.

## Plan and implementation boundaries

Independent slices reuse the existing Rust workspace: production Git process
cleanup, raw configuration listing, and MCP tool catalog/list-time
filtering. Shared gates and fixture provenance are integrated together after
focused checks. Tool listing does not imply tool execution or authorization
parity; CLI family progress does not close the entire CLI contract.

## Git and native execution

The Go system-Git adapter assigns a suspended Windows child to a kill-on-close
Job Object before resuming it. Setup failures kill and reap the child, and
cancellation bounds pipe cleanup. The production adapter now has an explicit
native Windows CI step in addition to the differential harness tests.

The Git oracle advances to `28fd35315cf4989821a96bb08279c999c693e8d9`.
The regenerated fixture retains all five behavior projections unchanged;
its source/generator identities and the Rust fixture assertion advance together.
The reachability gate now includes generators using `oracleCommit` constants.
A missing-object negative control was rejected by the real gate.

Rust's Git adapter now assigns a suspended Windows process to a kill-on-close
Job Object before resuming its primary thread. Targeted Windows cross-Clippy
passes; native descendant and inherited-pipe runtime tests for this new adapter
still require final-head Windows evidence. Older Go checks do not close this gate.

## Local storage

Build targets, Cargo/Go caches, test sandboxes and temporary files are on the
external NVMe. The native closeout runner now honors an explicit `TMPDIR` on
macOS instead of unconditionally choosing `/private/tmp`.

## Verification at the Git integration checkpoint

- Existing `make port-contract`: passed with Rust 1.98.0 / Go 1.26.6 and external
  storage before source integration (189 seconds).
- Integrated `internal/git` and Git fixture-generator Go tests, `go vet`: passed.
- Updated Rust Git IO contract: 10 tests passed; all-target sync Clippy passed.
- Workflow syntax, formatting, source reachability and missing-object negative
  control passed.
- Native production Windows process-tree, Go Windows/macOS and Rust
  Windows/macOS jobs passed on `3a3ef0f8` in Actions run `35239378698`.
  This is component evidence, not final-head acceptance. Linux jobs remain pending.

## Config review boundary

The initial `config get` candidate is not integrated: parsing into
`serde_yaml_ng::Value` normalizes YAML scalar spelling and resolves aliases,
whereas Go returns node text. `config list` remains the selected CLI slice
because it can preserve the source bytes without a new YAML parser.

## MCP catalog/list slice

The native handler supports `tools/list` after initialization, all 35 Go tool
schemas, original ordering/annotations, lean listing and injected capability/
profile filters. Eleven Go-produced scenarios include custom tiers with unset,
true and false value-exposure flags. The unset versus explicit-false distinction
also preserves the Go `get_entry` schema behavior.

The fixture verifies seven production-source files against `fca3f894`, hashes
its generator, and is checked by `make mcp-list-differential` and `port-contract`.
The integrated MCP suite passes 27 tests and all-target Clippy. Runtime
capability discovery, tool execution, policy authorization and approval remain
outside this slice; MCP-002 is in progress and RUST-010 stays blocked.

## Raw config list slice

Native `config list` preserves raw file bytes, including malformed YAML, CRLF,
empty files and invalid UTF-8. Thirteen real Go CLI subprocess cases cover
quiet mode, global flags, default HOME and missing-file/path errors. The fixture
compares stdout bytes and exit status; diagnostic matching is a stable substring,
not a byte-exact stderr contract. `config get` and mutating commands remain open.

The oracle executable must carry clean `fca3f894` VCS metadata and Go 1.26.6.
Build it from an ordinary isolated clone: linked worktrees produced incorrect or
missing Go VCS metadata on this host. The generator rejects those binaries.

## Combined local verification

The combined Rust workspace/all-features test run passed, with the two existing
native keychain/daemon tests deliberately ignored. CLI/MCP all-target Clippy
with warnings denied and workspace formatting passed. Native platform evidence
above belongs to the earlier component commit; final-head CI is still required.

The integrated `make port-contract` passed, including fixture freshness, Git/
pairing/crypto differentials and crypto fuzz smoke. The added raw CLI gate is
`make config-cli-differential`; it builds a clean pinned oracle clone under the
caller-selected TMPDIR and validates executable metadata before replay.

## Laufender Integrationsstand 2026-09-17 16:10 UTC

- Gepushter Kandidat `4a1cac2d`: Rust-Windows-Job-Object-Adapter, native OS-Keyring-Adapter samt Go→Rust→Go-Probe und zusätzliche Importparität integriert. Go bleibt produktiver Backendstand.
- CSV/Browser/Bitwarden: 27 reale Go-Importfälle, 20 TOTP-Fälle sowie Pfad-/Präfixvektoren; Rust-Tests lokal bestanden. TOTP-Base32-Grenzfälle korrigiert.
- Windows-Rust-Prozessbaum: Cross-Clippy bestanden; die neue native Laufzeitprüfung bleibt offen. Frühere Windows-Erfolge sind keine Abnahme dieses neuen Rust-Adapters.
- CI auf `4a1cac2d`: Audit-/Storage-Differential erfolgreich; Windows-Go-Prozessbaum und Vaultcore macOS erfolgreich. Allgemeine CI noch nicht grün: Go-Lint selbst 0 Issues, gosec meldet synthetische Fixture-/CI-Probe-Stellen; gezielte begründete Kennzeichnungen in Arbeit.
- MCP-Ausgabebereinigung lokal implementiert: Go-orakelgebundene 26 Angriffs-/Unicodevektoren plus Hashvergleich aller gültigen Unicode-Skalare bestanden. Unicode 15 und Stream-Safe-Normalisierung sind für Go-1.26-Parität nötig. Randomisierte Datenmarker verwenden OS-Zufall ohne unsicheren Fallback.
- CXF und 1Password/pass liegen als Agentencommits vor; noch nicht integriert. Review verlangt weitere Null-/Typ-/Pfad-Grenzfälle. YAML get/set bleibt in Arbeit, keine Abnahme der verworfenen Emitter.
- Alle lokalen Builds, Tests, Fixture-Prozesse, Downloads und temporären Dateien weiterhin NVMe; `dev-external --status` bestätigt Mount und 8 Links. Keine produktive Schlüsselbundprobe lokal.

Nächster Schritt: korrigierte Importer integrieren, MCP-Prompts anschließen, kombinierte Tests und native Head-CI auswerten. Gesamtmigration weiterhin offen (vollständige CLI/TUI, MCP-Ausführung/HTTP/OAuth/Broker, Swift-Bridge, Distribution/Value/Rollback und spätere Release-Gates).

## Integrated importer and MCP checkpoint

At `22386768`, the complete Rust workspace/all-features tests, workspace
all-target Clippy, and `make port-contract` passed locally on the NVMe.
The later MCP dispatch slice adds prompts/list and prompts/get with 25 real Go
protocol cases, typed/null/casefold parameter handling, and validated randomized
data boundaries. Diagnostic text alone is normalized as in the existing MCP
contract; result payloads and marker counts are compared completely.

`4373522d` native CI passed Rust macOS/Windows, Go macOS/Windows, and lint.
Linux found two distinct issues: the new render generator used an unrecognized
pin name (now fixed and negative-tested), and the Unix timeout test treated a
killed orphan zombie as a live process. Process-group kill now uses an explicit
option terminator, and Linux probes distinguish zombie state while failing on
unexpected proc inspection errors. Native keyring staging no longer invokes
Windows WSL through bash; direct Python argv stages preserve fail-fast behavior.
These later corrections still require native acceptance on their own head.

Config get/set remains unintegrated following review findings; source-bound
passing samples alone are not acceptance of the complete YAML editing behavior.

## CXF integration and current CI evidence

CXF is wired into the shared importer dispatch. Nineteen source-bound Go cases
cover archive selection, malformed and null typed fields, credential warnings,
and structured TOTP edge values. Structured TOTP now uses the common validator
directly, avoiding URL reinterpretation of secrets and algorithms. Core/sync
tests, focused differential checks and native Clippy passed locally on NVMe.
The archive fixture writer now includes every requested filename rather than
silently dropping unknown names. IO-001 remains in progress for full parity.

At `6c0bbcca`, CI run `35246934508` passed the Rust port contract, Rust macOS,
Go Windows and Windows process-tree jobs. Its macOS Go find test and gosec failed;
the latter was an annotation placement issue on the synthetic executable GPG
fixture, now corrected. macOS find is under investigation. Linux pairing in run
`35246939742` passed; final-head native platform acceptance remains open.
Config get/set and session CLI candidates remain unintegrated pending review.

## Native adapter follow-up

Linux and Windows hosted cross-language keyring artifacts passed on PR merge
`ffe087d1` (head `6c0bbcca`) in run `35246939742`. macOS reached the Rust read
after a successful Go write and then hung. The macOS provider now uses the
same `/usr/bin/security` executable as Go, preserving existing access controls
and the legacy/current payload codec, with secrets on stdin and a five-second
deadline. The 4096-byte interactive command limit is checked before spawning.
Native macOS interoperability is still pending; no user keychain probe was run.

The shared macOS helper now services stdin/stdout/stderr without blocking and
terminates its process group on timeout. A synthetic local pipe test verifies
large bidirectional traffic, blocked input and inherited descendant pipes.
No secret-bearing environment is forwarded; HOME is retained for isolated
keychain routing. Platform tests and Clippy run entirely on the NVMe.

## Expanded export and focus contracts

The JSON/CSV export review found missing nested map/array formatting, Go HTML
escaping, CSV leading-whitespace/sentinel quoting, column mapping and attachment
notices. Five additional production-Go cases now pin those bytes; output and
notice writer failures are checked separately. IO-002 is corrected from a narrow
PASS to in_progress: numeric edges, mapping collisions and full CLI
coverage remain open.

Autotype now calls the Go-equivalent two-capture focus guard before sending
text on macOS. Ten source-bound cases cover strict/lenient detection failure,
changed/empty focus and provider errors. No local GUI/autotype operation ran.

Hosted keyring interoperability now passed on all three OSes in run
`35249056550` (PR merge `3fab62b9`, head `bc62c5cb`); artifacts prove the actual
Go-write/Rust-read-update/Go-read-delete sequence. Full local workspace tests,
port-contract and Clippy passed after the native-helper integration, with later
focus/export changes validated separately. This is not a complete migration gate.

## Integrated config and session CLI checkpoint

Config get/set now passes 75 real Go CLI cases, including explicit block-scalar
indentation, nested sequences, comments and post-write validation. The integrated
config tests, source-bound oracle freshness check, Go lint and Rust Clippy pass.
Session lock/status/check and passphrase unlock are integrated; CLI/platform tests
and Clippy pass. Touch ID unlock, TTL flags and complete command parity remain
in progress.

Restore no longer writes to a predictable sibling temporary filename. It uses
the existing tempfile dependency with exclusive creation, descriptor-based mode
setting and no-clobber publication when overwrite is false. A regression verifies
that a planted temporary-name symlink cannot alter an unrelated file; archive
contract tests and Clippy pass. This does not claim to close all restore races.

## CSV byte-level follow-up

The CSV/browser corpus now contains 36 production-Go cases. ByteRecord preserves
invalid UTF-8 until Go-compatible JSON replacement, including BOM handling, raw
header identity and a single exact/lowercase header map. Raw bytes also govern
profile path de-duplication. The public ImportedEntry path is still UTF-8: the
filesystem identity of distinct malformed byte paths is not yet represented by
that API, so import-command parity remains open. The Rust 100 MiB input cap is
also an unresolved difference from Go CSV, not an approved parity exception.

At be1f5d05, make port-contract, full workspace/all-features tests, all-target
Clippy and formatting passed locally. Later CSV integration receives focused
Go differential, sync tests and lint before publication.

## Connected CLI and MCP execution checkpoint

`299c707e` integrates native init/list/get, session TTL/cache identity handling,
concrete encrypted-store MCP read-only calls, flushed stdio transport, and the
export execution module. The four CLI/session nextest checks (including the real
pinned Go binary) and ten MCP call tests passed. MCP Go fixtures and five export
checks passed; export cancellation cannot unlock or create output, and CSV column
mapping reuses the existing formatter without collapsing duplicate columns.
The config CLI oracle gate now also runs the init/list/get subprocess comparison.
MCP serve configuration and export CLI registration remain in the active batch.

At remote `1a077746`, native pairing/keyring, audit, and storage workflows passed.
General CI failed on the config-generator gosec annotation and a session fixture
with inherited environment/shared case roots. Both local corrections are now
integrated; the next pushed head still requires CI. No release/cutover is implied.

## Connected CLI checkpoint and execution speed

At `c5b94ef1`, the workspace/all-features nextest run passed all 450 tests in
34.221 seconds after a 25.83-second build. Three native credential/daemon tests
remain explicitly excluded on this developer machine. Workspace doctests and
`make port-contract` also passed. At `cb168dd3`, all-target/all-features workspace
Clippy and formatting passed after mechanical CLI dispatch lint corrections.

The connected CLI now includes init/list/get, set/delete, config/session,
import/export and MCP stdio. Actual pinned-Go subprocess checks cover CRUD,
CSV import, export bytes, MCP initialization and export cancellation. CXF
secret type metadata is retained; a failed replacement preserves the old entry.
Atomic replacement intentionally differs from Go's delete-before-write failure
behavior and intermediate Git history; this is not an exact overwrite-contract
parity claim. Five MCP read-only tools use the encrypted store, scope/policy and
audit adapter. Remaining tool families and transports are still open.

The acceleration changes reuse a verified Go binary, avoid repeated sync suites
within the contract target, and optimize existing KDF dependencies only in the
test profile. All nine sync integration suites and library checks remain in the
contract target's dependency graph. Production KDF parameters are unchanged.
A synthetic cache test now uses a cheaper fixture envelope (0.66 seconds after
the prior unoptimized attempt exceeded 70 seconds and was stopped). Local CLI
subprocesses run with in-memory credential isolation. The session fallback test
now follows Go's explicitly opted-in environment fallback after unavailable
Touch ID, while still rejecting an explicit environment-passphrase prohibition.

Current-head hosted CI remains required. Go remains the production backend;
full TUI/CLI, MCP/HTTP/OAuth/broker, Swift and release/value/rollback gates remain.

## Connected follow-up after 607831d9

- Search/find supports the existing encrypted index, URL host filtering and
  Unicode case normalization; focused real Go/Rust CLI comparison passed.
- Generate/gen is dispatched through the existing password implementation and
  encrypted Store mutation path. Get now emits TOTP diagnostics and structured
  TOTP fields through the fixed-clock-tested helper.
- Git history and recipient mutation helpers are integrated; dispatcher and
  recipient decryption/revocation checks are still in progress.
- MCP get_entry_value uses the source-bound Go fixture, profile scope, quarantine,
  field/payment redaction, classification sealing and session counters. Interactive
  approval and semantic injection checking remain incomplete; required unsupported
  paths must fail closed. No full MCP parity is claimed.
- Published 607831d9 passed storage, audit and pairing CI. General/native CI exposed
  host-dependent MCP capability fixtures, macOS /var versus /private/var test-path
  normalization, and non-macOS unused Touch ID code. Local corrections are integrated;
  a fresh native run is still required before reporting the batch green.
- New tests, caches and temporary vaults remain exclusively on the mounted NVMe.

## Fortschritt: native Plattformprüfungen grün, nächste Funktionswelle lokal

Gesamtmigration weiterhin grob **50–55 %** nach geschätztem Restaufwand, keine gemessene Abnahmequote. Für den gepushten Stand `cd7b4a2d` sind die nativen Rust-Jobs auf macOS und Windows sowie der Port-Contract in Run `35262732910` erfolgreich; der gesamte Lauf ist noch nicht abgeschlossen.

Lokal zusätzlich integriert: Backup/Restore-CLI mit echtem Go↔Rust-Archiv-Roundtrip, Streaming-Archive mit Go-Restore-Größenlimits (1 GiB pro Datei, 16 GiB gesamt), MCP-Passwort- und TOTP-Generierung. Archivverträge: 9 Tests und Clippy erfolgreich. Die TOTP-CLI-Freigabe und gemeinsame MCP-Gates werden gerade geprüft. Kein vollständiger Abschluss dieser späteren Änderungen durch ältere CI behauptet.

Beschleunigung: native Rust-Jobs werden künftig direkt in der PR-CI ausgeführt und im Pflicht-Gate berücksichtigt; kein zweiter kompletter manueller CI-Lauf pro Push nötig. Drei Luna-High-Lanes arbeiten weiter an Add/New/Create, MCP-Parität und gemeinsam gesperrter, absturzsicherer Schlüsselrotation. Alle lokalen Runner-, Test-, Cache- und Temp-Ausgaben auf NVMe. Der sporadische leere Go-URL-Suchoutput bleibt diagnostiziert offen, trotz später erfolgreicher Läufe. Vollständige CLI/TUI, weitere MCP-Tools, HTTP/OAuth/Broker, Swift und Gesamt-/Release-Abnahme fehlen weiterhin.

## Aktiver Stand: 50–55 % geschätzter Gesamtaufwand, nächste Welle vor Push

Gepusht weiterhin `cd7b4a2d`: PR-CI `35262737448` vollständig erfolgreich. Im expliziten Vollmatrix-Lauf `35262732910` bestanden native Rust macOS/Windows und Port-Contract; der Lauf scheiterte ausschließlich an Go-Gesamtcoverage (62,8 % < 63,5 %), nicht an einem Go-Test. Diese Belege gelten nicht automatisch für die späteren lokalen Änderungen.

Lokal `0d07cd34`: Backup/Restore, Add/New/Create ohne interaktives Formular und Verify verbunden; MCP neun produktive Tools einschließlich Password/TOTP-Return. Streaming-Archive und stderr-Warnung für semantic log-only integriert. Audit-view-Helfer mit echtem Go-Differential fertig, CLI-Anschluss folgt. Vorheriger Paketlauf: 477 Tests bestanden, drei native Ausnahmen, 23,737 Sekunden. Aktualisierte Workspace-/Doctest-/Clippy-/Port-Contract-Prüfung läuft.

Go-Coverage: neue Fixture- und Provenienztests prüfen echte Fixtures sowie Isolation, Rohbytes und falsche Oracle-Claims. Kombinierte lokale Paketprofile zeigen 63,587 %, alle fünf sicherheitskritischen Paketgrenzen bestanden; Linux-CI-Beweis noch offen. Keine Schwelle abgesenkt. Coverage wird nun im vorhandenen PR-Testlauf geprüft; native Rust-Jobs ebenfalls direkt in PR-CI. Doppelte native Typecheck-/Doctest-Aufrufe entfallen bei gleicher Prüfabdeckung.

Drei Luna-High-Lanes: File add/get (Korrekturen bei Feldpriorität, Chunkanzahl, Metadaten/Git); MCP set_entry_field (Merge/Limit korrigiert, persistierte Go-Metadaten/Git noch im Abgleich); gemeinsame Rotationssperre und Crash-Journal (Pfadsicherheit korrigiert, erfolgreicher Artefakt-Cleanup und Go-Journal-Namenskompatibilität noch vor Integration). Diese Agentencommits sind noch nicht als fertige Gesamtslices integriert.

Gesamtmigration grob 50–55 % als Aufwandsschätzung. Vollständige CLI/TUI, weitere MCP-Tools, HTTP/OAuth/Broker, Swift, native Interaktion und Release-/Gesamtabnahme bleiben offen. Sporadischer leerer Go-URL-Suchoutput weiterhin keine bewiesene Ursachenbehebung. Alle lokalen Runner-/Test-/Cache-/Tempdaten ausschließlich NVMe; keine echten Vault-/Keychain-Probes. Go bleibt Produktion, kein Release/Cutover/Go-Entfernen.

## Aktiver Stand: Integration weiterer CLI-/MCP-Funktionen, Gesamtmigration etwa 50–55 %

Lokal bis `9f7d18a6`: File add/get/use, Audit-CLI, Verify, Backup/Restore und nichtinteraktives Add integriert. MCP elf produktive Handler einschließlich set_entry_field/delete_entry; gemeinsame Git-Autocommit-Implementierung für CLI/MCP integriert. Crash-Journal-Recovery im Store integriert; echte Go↔Rust-Absturz-Wiederherstellung noch in Prüfung. Die Prozentzahl bleibt eine Aufwandsschätzung, keine gemessene Abnahmequote.

Prüfstand: vorherige Welle `0d07cd34` bestand Workspace-Clippy, 479 Nextests (drei native Ausnahmen), Doctests und gesamten Port-Contract. Die neue integrierte Welle ist noch nicht grün: Clippy meldete eine verschachtelte Bedingung im Rotationspfad; zwei vorhandene Tests erwarteten nach der Store-Anbindung alte Fehlertexte für unsichere entries-Verzeichnisse. Ursachenprüfung/Korrektur läuft. File-use-Review fand zusätzlich Risiken bei symlinkbasierter Bereinigung und vererbten Child-Pipes bei Timeout; vor Push zu beheben. Einzelne erfolgreiche Tests ersetzen diese offene Gesamtabnahme nicht.

Remote bleibt `cd7b4a2d`: PR-CI 35262737448 erfolgreich; explizite Vollmatrix 35262732910 nur wegen Go-Coverage 62,8 % < 63,5 % fehlgeschlagen. Native Rust macOS/Windows bestanden für diesen alten Head. Neue Generator-/Provenienztests ergeben lokal kombiniert 63,587 % ohne abgesenkte Schwellen; neue Linux-CI-Bestätigung steht aus.

Beschleunigung: drei GPT-5.6-Luna-High-Lanes, wiederverwendeter Go-Oracle, gezielte Slice-Prüfungen plus ein integrierter Wellenlauf. Native Rust und Go-Coverage direkt in PR-CI, doppelter Pairing-Pushlauf entfällt. Alle lokalen Build-/Test-/Cache-/Temp-Ausgaben auf NVMe. Keine echten Vault-/Keychain-Probes. Vollständige CLI/TUI, weitere MCP-Tools, HTTP/OAuth/Broker, Swift, native Interaktion und Release-/Gesamtabnahme offen. Go bleibt Produktion; kein Release/Cutover/Go-Entfernen.

## Aktiver Stand: lokale Integrationswelle e415ed9c, Gesamtmigration etwa 50–55 %

Gesamtfortschritt weiterhin grob 50–55 % des geschätzten Aufwands, keine gemessene Abnahmequote. Remote bleibt cd7b4a2d (PR-CI, Pairing, Audit und Storage erfolgreich; zusätzliche Vollmatrix nur wegen Go-Coverage 62,8 % < 63,5 % fehlgeschlagen). Alle späteren Änderungen sind lokal und noch nicht am neuen Head nativ abgenommen.

Neue integrierte Funktionen: File add/get/use, Audit-Anzeige, Verify, Backup/Restore, nichtinteraktives Add, Editor-Dispatch, fünf eingebaute Template-Formate und migrate kdf. MCP zwölf Handler einschließlich Schreib-/Löschoperationen und Authentifizierungsstatus; gemeinsame Git-Autocommit-Implementierung für CLI/MCP. Custom Go-Templates und nicht-stringbasierte Template-Referenzen werden ausdrücklich abgelehnt und sind weiterhin offen.

Belege: letzte vollständige Welle 0d07cd34 bestand 479 Nextests, Clippy, Doctests und Port-Contract. Zwischenlauf mit späteren Änderungen: 497/499 bestanden; die zwei Fehler wegen entries-Root-Diagnostik wurden gezielt korrigiert. MCP-Differential einschließlich Auth-Status und Go-Fixture-Prüfung bestanden. Fünf Template-Formate stimmen für direkte Referenzen, Prefix und Dry-run bytegenau mit Go überein. Editor-Rundlauf gegen Go bestanden. Go-Produktion erzeugt Crash-Journal, Rust öffnet/entschlüsselt erfolgreich; Rückrichtung bisher Rust-Formatadapter zu Go-Recovery, kein echter Absturz im vollständigen Rust-Befehl.

Neu gefundene Ursache: Rust-init schrieb einen leeren vault-YAML-Block, den der Rust-Loader selbst ablehnte; gemeinsamer Config-Loader/Writer korrigiert, frischer init→set→template-Test und vollständige Core-Suite erfolgreich. Go-basierte Nachprüfung expliziter false-Werte läuft, damit deaktiviertes Auto-Push/Clipboard nicht wieder aktiviert wird. Gemeinsamer safeio-Helfer wird gegen Symlink-Rennen und vorhersagbare Staging-Dateien abgesichert; begrenztes Editor-Einlesen folgt. KDF-CLI ist verbunden, vollständiger Go-CLI-Vergleich folgt.

Beschleunigung bleibt aktiv: drei GPT-5.6-Luna-High-Lanes; fokussierte Slice-Prüfungen und ein integrierter Abschlusslauf pro Welle. Native Rust und Go-Coverage direkt in PR-CI, kein doppelter Pairing-Pushlauf. Lokale Runner/Tests/Caches/Tempdateien ausschließlich NVMe. Wegen Platzdruck nur eigene, nicht laufende incremental-Compiler-Caches bereinigt; Quellcode/Git/Nachweise erhalten. Keine echten Vault-/Keychain-Probes. Vollständige CLI/TUI, weitere MCP-Tools, HTTP/OAuth/Broker, Swift und Release-/Gesamtabnahme offen. Go bleibt Produktion.

## Aktiver Stand: Integrationsprüfung 2026-09-17, Gesamtmigration etwa 50–55 %

Schätzung nach Gesamtaufwand; keine gemessene Abnahmequote. Remote weiterhin cd7b4a2d, lokale Folgewelle noch nicht gepusht oder nativ abgenommen. Go bleibt Produktion.

Neue Belege: Workspace-Clippy bestanden; vollständiger Nextest-Lauf 514/514 erfolgreich, vier Ausnahmen. Vorheriger Lauf zeigte eine intermittierende Suchabweichung, die durch den erfolgreichen Wiederholungslauf nicht als behoben gilt. Root cause: ein persistierter Index prüft nur Eintragsanzahl; Go-Schreibzugriffe können bei parallelem Warmup einen gleich großen Index veralten lassen. Rust CLI scannt jetzt aktuelle Einträge (O(n), bis der gemeinsame Index Aktualität beweisen kann); gezielter Stale-Index-Regressionstest bestanden. Der CLI-Go-Vergleich entfernt vor der Go-Suche nur den synthetischen Index und prüft frische Suchergebnisse; persistente Index-Verträge bleiben separat. Aktualisierter CLI-Vergleich bestanden.

Profile list einschließlich Unicode-Spalten gegen tatsächliche Go-Ausgabe bestanden. Config-Writer-Fixtures decken Go SaveTo/Load ab; Rust erhält explizite false-Werte für Git/Clipboard absichtlich auch dort, wo Go omitempty sie verliert. Safeio verwendet CLOEXEC/NOFOLLOW sowie FIFO-Abweisung; Editor führt den aufgelösten Pfad aus.

KDF-Differential ist jetzt expliziter Teil des Port-Contract und findet noch ungültige YAML-Einrückung nach Entfernung des scrypt-Felds. Reparatur und erneute integrierte Prüfung laufen; kein grüner Gesamtstand behauptet. Native CI für den neuen Head bleibt offen.

Alle lokalen Runner, Builds, Tests, Caches und temporären Daten bleiben auf NVMe; nur eigene inaktive inkrementelle Compiler-Caches wurden bei Platzdruck entfernt. Drei Luna-High-Lanes arbeiten weiter. Vollständige CLI/TUI, MCP-Tools/HTTP/OAuth/Broker, Swift, Plattforminteraktion und Release-/Gesamtabnahme bleiben offen.


## Aktiver Stand: geprüfte Integrationswelle, Gesamtmigration etwa 50–55 %

Schätzung nach Gesamtaufwand, keine gemessene Abnahmequote. Stand vor dem nächsten Push: lokale Codewelle bis 7a468f0a; Remote noch cd7b4a2d. Go bleibt Produktion, PR1069 Draft, kein Release/Cutover/Go-Entfernen.

Neue Belege: Workspace-Clippy und 520/520 Nextests erfolgreich (vier bewusst übersprungen, drei LEAK-Markierungen wegen nachlaufender Ausgabekanäle separat in Untersuchung). Doctests bestanden im vorherigen Teillauf. Alle Port-Contract-Ziele wurden über den initialen Lauf, den Restlauf und den erneuten Lauf der geänderten Ziele abgedeckt. Geänderte Ziele: reencrypt-journal-differential, config-cli-differential und mcp-call-fixtures-check bestanden. Der reale KDF-Go/Rust-Vergleich ist obligatorisch; fünf KDF-Unit-Tests einschließlich Flow-YAML/unknown-fields bestanden.

Profile list/add/use sind verbunden; echte Go-Vergleiche einschließlich Unicode und Punkt-/Klammernamen, Quiet-Modus und Fehlerausgabe bestanden. Rust validiert vor einer einzigen atomaren Veröffentlichung, erhält unbekannte semantische Felder und lehnt bestehende defekte Konfigurationen unverändert ab. YAML-Kommentare/Formatierung werden bei strukturellen Änderungen normalisiert. Config-Writer erhält explizites false für Git/Clipboard als dokumentierte Sicherheitsabweichung zu Go omitempty.

MCP nun 13 produktive Handler einschließlich symaira_audit_self. 15 tatsächliche Go-Fixturefälle decken Limit, null/leere/fehlende Logs, ungültiges UTF-8, alle typisierten Audit-Felder, Groß-/Kleinschreibung, HTML-Escaping und 64-KiB-Zeilengrenze ab. Der Leser verwendet den gemeinsamen sicheren Dateideskriptor und begrenzten Tail-Speicher. Vorhandene MCP-Fixture-Gates bleiben erhalten und sind um AuditSelf ergänzt.

Suche: Rust CLI scannt aktuelle Einträge (O(n), bis der persistente Index Aktualität nach externen Go-Schreibzugriffen beweisen kann). Ein reproduzierbarer Test schützt vor veraltetem Index bei gleicher Eintragszahl. CLI-Go-Suchvergleiche verwenden ausdrücklich einen frisch aufgebauten synthetischen Index; persistente Indexverträge bleiben separat. Der bisherige intermittierende Go-Cachefehler wird nicht als Go-Produktionsfix ausgegeben.

Crash-Recovery: Go-produzierte Journale werden von Rust wiederhergestellt. Zusätzlich wird jetzt der tatsächliche Rust-Rotationsprozess nach Veröffentlichung seines Journals beendet; Go stellt danach alle acht vollständigen Testeinträge wieder her und räumt Journal/Artefakte auf. Das beweist lokale macOS-Journalveröffentlichung und Wiederherstellung, nicht sämtliche Crash-Zeitpunkte oder native Windows-Ausführung.

Alle lokalen Runner, Tests, Builds, Caches und Tempdaten weiterhin NVMe. Wegen Platzdruck nur eigene inaktive Compiler-Zwischendateien gelöscht. Drei Luna-High-Lanes arbeiten weiter an Remote-Status, MCP-Sanitization und Prozess-/Pipe-Nachprüfung. Vollständige CLI/TUI, weitere MCP-Tools/HTTP/OAuth/Broker, Swift, Plattforminteraktion und Release-/Gesamtabnahme bleiben offen.


## Follow-up after 8773104e

Local remote status now passes the actual pinned-Go differential; sanitize_output is the fourteenth connected MCP handler and the complete mcp-call-differential target passes. Sync dispatch is connected but force handling still fails focused checks; it is not accepted as complete. The worker is implementing conflict preservation and correcting local-error/offline classification.

CI 35273529493 at 8773104e confirms Ubuntu Go PR tests, Linux build, lint and Windows process-tree checks. Windows Rust fails on a Unix-only test import (locally guarded); macOS Rust exposes lexical /var versus canonical /private/var journal paths; Linux production-journal fixture exceeds its preflight deadline. Root-cause and fixture-size repairs are in progress. No full green claim for this head.

## Second integrated follow-up — 2026-09-17

Remote status and sync are connected and covered by actual pinned-Go CLI differentials. Sync preserves conflicting local bytes before a force reset, refuses existing different conflict copies and remote ancestor collisions, and preserves ordinary divergent-pull snapshots. Rust deliberately retains the stable device ID that go-git can delete during reset; the differential compares preserved contents while accounting for Go's regenerated filename. The Go config-conflict warning off-by-one is retained as an output contract. Ordinary pull currently snapshots tracked candidate contents (O(vault bytes)); a follow-up replaces clean-file snapshots with immutable Git objects.

The productive MCP runtime has fifteen connected handlers plus masked built-in generate_template previews. Templates reuse the existing sync renderer; actual secret resolution, custom templates and output_path are explicitly unavailable in this slice. MCP fixture freshness and all handler differentials pass. Remote JSON/YAML edge cases include Unicode separators and multiline URLs.

Native CI repairs: Unix-only test import is cfg-gated for Windows; journal roots accept platform aliases while rejecting journal-internal symlink ancestors; the production crash fixture uses eight complete 512-KiB entries instead of 64 MiB total. The file-use timeout test handles expiry before shell startup and cleans its own fixture with RAII.

Validation: workspace all-target/all-feature Clippy passed; 529/529 nextests passed in 96.733 seconds, four platform tests skipped and three leaky-output annotations remain under investigation (different tests than the earlier run). All ten git_io_gaps tests and the force-copy/remote-ancestor regression passed. Workspace doctests and the final actual Go/Rust journal recovery check (8.906 seconds) passed. These checks do not claim native Windows/macOS CI acceptance for the new head.

Previous head 8773104e: Linux Rust gates, Ubuntu Go tests and build, lint, Windows process tree, audit 35273529489, pairing 35273529488 and storage 35273527920 passed. CI 35273529493 failed only the three native/journal jobs described above and its summary gate. Go remains production; PR1069 stays draft.

All local artifacts remain external. Only the inactive debug compiler outputs of the three migration worker worktrees were removed to recover space; sources, Git state and evidence files were retained.

### Pull snapshot optimization after the full-suite checkpoint

Ordinary pull now snapshots only dirty tracked candidates, including staged edits and deletion tombstones; clean remote-touched data comes lazily from pre-pull Git objects. Nested source/conflict paths reject symlink/non-directory ancestors below the trusted repository root. This is an lstat preflight plus final-target safe I/O, not a claim of TOCTOU-proof filesystem access.

After this delta: sync library plus all ten Git I/O regressions passed, both actual-Go sync CLI cases passed, the staged-edit/deletion regression passed, and Clippy for all affected crates/targets/features passed. The earlier full 529-test run predates these final targeted changes; fresh native CI remains the full-head acceptance gate.

## Third follow-up — 2026-09-18

Audit export now reuses the store exporter and existing OS-keyring codec through a shared loader. CLI rendering preserves Go JSON null/omitempty, HTML escaping, table timestamps, filters, file output and stderr summaries. Actual Go/Rust CLI comparisons and the synthetic HMAC/keyring-loader tests pass. Output files use existing private atomic publication.

Agent profile show and read-only policy validate/list are connected. Actual Go differentials cover YAML/JSON, explicit empty fields, escaping, policy validation failures, empty/missing directories, directory filtering and tilde paths. These are partial command-family implementations, not complete agent/policy administration.

MCP search/fetch now share existing search, sealing, redaction and session-limit logic. Thirteen source-bound Go cases pass, including value exposure and session limits. Search preserves Go path-first ranking. Fetch deliberately applies configured get policy to its id and stays metadata-only when the filtered registry excludes get_entry_value; Go's path-only middleware/independent exposure flag is less restrictive. Store audit checks cover both handlers and denied fetch policy. Existing policy action mapping is corrected for value reads and generation.

Rate-limit investigation: Go profile hourly/day counters are informational in MCP. A manually registered pre-call hook enforces a fixed one-minute limit. The pinned Go configuration merge also drops pre_call_hooks/post_call_hooks, so Rust must not silently enable them from YAML while claiming parity. The injectable runtime hook now passes three actual-Go protocol scenarios, first-call/reset tests and fail-closed policy-denial coverage; no persistent/hourly quota enforcement claim is made.

Remote 2ec986cd: audit CI passed; primary native macOS/Windows jobs still failed journal root alias validation; pairing Windows additionally rejects the test's newline filename, and storage macOS exposed a second root-alias validation path. The Linux coverage rerun additionally hit procfs ESRCH while observing an already-exited descendant; the test now accepts disappearance during the read. These are repaired locally with targeted regressions before another native CI run. No final-head native acceptance claim.

Storage pressure came from simultaneous external build workloads. Only owned, inactive compiler caches/artifacts were pruned: integration incremental caches, old test executables and old object files; source, Git, fixtures, evidence, dependency libraries and recent executables were retained. All local builds/tests/temp remain on NVMe. Go remains production and PR1069 remains draft.

Third-wave focused validation: five connected CLI differential tests (including synthetic HMAC export), thirteen CLI device tests, the store alias/symlink regression, the Unicode/space/newline conflict-copy regression, MCP library and eight store-backed dispatch tests, and the complete make mcp-call-differential target pass. No additional full-workspace Nextest rerun was performed for this wave; native CI on the next pushed head is still required.

Clippy passed for all targets/features of CLI, MCP, store and sync after this third-wave integration.

## Fourth integration wave — 2026-09-18

- Connected `remote init` and `policy apply/remove` to the Rust CLI. Remote setup preserves Go target parsing, auto-push configuration, quiet output and origin push behavior. Policy application writes the exact validated bytes atomically; removal unlinks without overwriting hard-linked contents.
- Actual Go/Rust remote and policy CLI differentials passed (three test functions); four policy safety tests and all-target/all-feature CLI Clippy passed. Build/test artifacts remain on external NVMe.
- CI on c8e1e9a1: pairing differential passed. General Rust jobs exposed an agent-profile YAML unit expectation failure; storage macOS exposed a lexical/canonical path expectation. The latter is corrected with its focused regression passing. Neither result implies final-head native acceptance.

## Fifth integration wave — 2026-09-18

- Connected non-mutating `migrate paths` / `migrate xdg` preview. Actual Go/Rust CLI comparisons cover empty/complete layouts, migration markers, quiet output, aliases and top-level/nested symlink diagnostics (two passing tests).
- All 36 current CLI unit tests and all-target/all-feature CLI Clippy passed. Fixed Unix-only policy test constant gating after Windows Clippy exposed its unused Windows definition.
- On 95da55b0, native storage and audit differential workflows passed. General CI is still not accepted because of the above Windows warning; the next push must confirm the correction.
- Rejected incomplete MCP auth-method mutation: biometric challenge/session cleanup must precede config mutation. No incomplete auth handler was integrated.

- Agent profile export now reuses the YAML show renderer for stdout/file output (empty output means stdout). The existing actual-Go differential now includes export content and missing-profile diagnostics for show/export; it and all-target CLI Clippy passed. Legacy path preview is included in the existing config CLI differential gate.

## Sixth integration wave — 2026-09-18

- Connected agent list: real Go text/compact JSON/YAML, token state and skill metadata differential passed. Multiple active tokens have Go map-order nondeterminism; Rust selects deterministically and no byte-exact multi-token-ID parity is claimed.
- Share metadata reader reuses bounded no-follow reads, implements last-ID-wins, scoped filters, Go HMAC fields/expiry semantics and safe malformed-ID handling. Eight focused tests passed. Its synthetic HMAC vector was independently reproduced by the real pinned Go GenerateGrantID implementation (share-actual-go-hmac.log). Legacy unsigned IDs remain Go-compatible; HMAC excludes lifecycle status/expiry, so no whole-record authenticity claim is made.
- list_shares is connected through the concrete MCP runtime, scoped to the caller before filtering, with share_list audit evidence. Helper and protocol/runtime tests passed. Current Go constructor leaves the share store unattached; Rust deliberately loads it at runtime construction and rejects malformed stores. This is documented behavior, not a claimed Go stdio differential.
- Session fixture path expansion now preserves JSON string escaping and native separators without dropping byte-order checks; all three local session-contract tests pass. Windows must confirm the portability correction.
- CLI/MCP/store all-target/all-feature Clippy passed before the subsequent share-CLI/run integration. That integration requires fresh focused checks; no new full workspace run claimed.

### Verified share/runner integration

- Share list and local revoke pass the actual Go/Rust differential, including exact Unicode/multiline/trailing-space JSON/YAML output and persisted revoke metadata. Only unordered multi-grant rows and dynamic revoke timestamps are normalized where needed.
- Revocation needs no identity or initialized vault. It reloads under the shared store lock and publishes atomically; the Windows lock opener rejects reparse points before permissions or locking. The agent-scoped API checks the current source agent inside that same lock. All 77 store unit tests passed, including stale-source, symlink, nonregular-target and unchanged-snapshot regressions. Windows must still confirm the new native path.
- MCP revoke_share is connected beside list_shares. Protocol tests cover invalid/missing IDs, recipient and cross-agent denial, persisted source revocation, relisting and audit. This remains Go source-guided runtime behavior; no Go stdio share oracle is claimed.
- Non-broker run now resolves secret environment mappings, supports env files/passthrough/working directory/timeout, and shares the bounded subprocess helper with file use. Native attachment paths remain OsString. Exact known-value masking reuses bounded merged spans (including short values); credential patterns preserve Go ASCII boundaries and whitespace semantics with Unicode-safe negated classes. The real Go/Rust subprocess comparison passes the built-in pattern corpus, overlapping secret values and exit behavior. Broker mode and complete command/flag/error parity remain open; no process-tree cancellation guarantee is added.
- Agent whoami reuses typed merged profiles and active token counting. Actual Go/Rust text/indented JSON, quiet/format fallback, null-path normalization and missing/unknown-agent comparisons pass.
- The shared environment-passphrase warning now matches the Go wording without changing its opt-in requirement. All 48 CLI unit tests and three session-contract tests pass. The complete existing config-cli-differential gate (including file-use timeout/cleanup and real KDF migration) and all-target/all-feature Clippy for CLI/MCP/store/core pass. Core's 67 unit tests passed before the final explicit Go-whitespace refinement; the actual Go corpus subsequently covers that refinement. No new full workspace run is claimed.
- All build/test/cache/temp outputs stay on the external NVMe. Under renewed space pressure only inspected, inactive artifacts within this integration target were removed, including old cross-build caches; source, Git state, fixtures and evidence remain intact.


## Seventh integration wave — 2026-09-18

Agent profile edit now uses the existing private editor temporary-file path, validates before atomic publication, preserves unrelated raw configuration, and refuses an observed concurrent configuration change. The confirmation/preview path and reordered fields match the pinned actual Go binary in the focused differential cases. The comparison immediately before replacement detects edits made during the editor session; it is not a cross-process compare-and-swap guarantee.

MCP request_share now validates required inputs, scope and Go duration syntax; creates pending grants under the root write lock; reloads current metadata; signs Go-compatible grant fields; and audits source-compatible outcomes. CLI stdio assembly loads the grant signing key through its injected keyring. Supported-platform valid legacy hex keys retain Go length semantics, including short keys. The unsupported-platform encrypted-file helper reuses bounded no-follow reads, the root lock and atomic publication; synthetic tests exercise creation, reload, plaintext migration and symlink refusal. Native unsupported-platform execution is not claimed. The existing platform keyring adapter retains its missing-item/provider-error distinction; unlike Go's grant-specific fallback, a missing item alone does not switch the Rust adapter permanently to memory. Reserved separator paths are rejected by the combined-address keyring interface. These boundaries remain explicit migration work.

Duration parsing reuses the shared parser and now preserves Go fractional-hour/minute nanosecond precision. A Go 1.26.6 probe independently confirmed the focused precision/error examples. Windows session fixtures omit filesystem-invalid angle brackets only when materializing synthetic Windows paths; ampersand/U+2028 escaping remains tested, Unix keeps the full original path, and pinned fixture/source digests are unchanged. Native CI confirmation remains required.

Local NVMe evidence in target/resume-evidence:
- wave7-config-cli-gate.log: full config-cli-differential gate passed, including actual Go/Rust profile edit, MCP stdio initialization and KDF integration.
- wave7-core.log: all 70 core unit tests passed.
- wave7-sharing.log: 18 focused sharing tests passed.
- wave7-grant-key.log: five keyring/encrypted-file/symlink tests passed, using synthetic identities and memory keyrings only.
- wave7-mcp-final.log: 12 MCP units, eight store dispatch tests and five sharing protocol tests passed.
- wave7-session.log: three session contract tests passed locally.
- wave7-clippy-final.log: CLI/MCP/store/core all-target/all-feature Clippy passed.

No complete workspace run or native Windows success is inferred from these focused results. On pushed predecessor 6ca2a513 native macOS, storage/audit/pairing differential gates passed; native Windows failed on the synthetic angle-bracket filename and is fixed locally above. Overall completion remains an estimated 50–55% of total effort. Production Go, Draft PR, no cutover/release/removal.

## Native RUST-007 closeout — 2026-09-26

At exact main head `a4e0469166f91e2a109e67a86df42e91f2d434d2`, rust-pairing run
`36050829754` passed. Its retained reports confirm Go-write/Rust-update/Go-read
OS-keyring interoperability on Ubuntu x86_64, macOS arm64 and Windows AMD64;
macOS also confirms the test used its provisioned private keychain. The ignored
macOS LaunchAgent test installed, queried and removed its job under a private
HOME. SESSION-002 is now PASS. RUST-007 remains in progress because its config,
session TTL/Touch ID and GUI contracts are incomplete.

A narrowly guarded macOS disposable-runner check now exercises clipboard
set/read/clear and clears the pasteboard on drop. CI execution is pending.
Hosted runners provide no biometric device for a real Touch ID decision, and
successful autotype/secure-UI delivery would require interactive Accessibility
permission plus a controlled foreground app; no safe unattended hosted-runner
proof is available for those paths. Clipboard timer behavior and autotype
remain unproven.
