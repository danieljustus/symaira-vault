{{template "symaira-skill" .}}
## Agent-Specific: Hermes

Hermes uses the `mcp_symaira_` tool prefix and `/symaira:` slash prefix.

To verify MCP connectivity:
```
hermes mcp test symaira
```

Hermes supports scoped tokens via `symaira mcp token create --agent hermes`.
