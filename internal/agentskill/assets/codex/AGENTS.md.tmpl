{{template "openpass-skill" .}}
## Agent-Specific: Codex

Codex uses `{{.ToolPrefix}}` as the tool prefix with no slash command support.

Codex agents should discover available tools on connection. Supported operations
follow the canonical tool list above.
