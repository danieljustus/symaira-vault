{{template "openpass-skill" .}}
## Agent-Specific: OpenClaw

OpenClaw uses `{{.ToolPrefix}}` as the tool prefix and `{{.SlashPrefix}}` for
slash commands.

OpenClaw supports native MCP tool discovery. Configure via the standard MCP
server setup in OpenClaw's configuration.
