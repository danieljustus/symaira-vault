{{template "openpass-skill" .}}
## Agent-Specific: OpenCode

OpenCode uses `{{.ToolPrefix}}` as the tool prefix and `{{.SlashPrefix}}` for
slash commands.

To connect OpenPass to OpenCode:
```
openpass mcp-config opencode
```

Use OpenCode's native MCP configuration for tool discovery and management.
