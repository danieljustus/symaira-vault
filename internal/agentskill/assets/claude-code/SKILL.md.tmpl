{{template "openpass-skill" .}}
## Agent-Specific: Claude Code

Claude Code uses the `mcp__openpass__` tool prefix and `/mcp__openpass__`
slash prefix.

To install OpenPass MCP in Claude Code:
```
openpass mcp-config claude-code
```

Claude Code supports native MCP tool discovery. If tools are missing,
re-run the configuration command above.
